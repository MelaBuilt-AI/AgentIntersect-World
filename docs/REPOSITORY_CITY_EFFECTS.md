# Repository City materialization and terminal rain

## Scope (2026-09-18)

Aaron authorized a separate private PR for city-object effects after checking PR16 merged-main CI. Run 35303937945 completed successfully on fed8c1524d23fca6edc3eee96d74d2ed935ccf72, attempt 1, all nine jobs successful. Historical failed attempts remain historical.

### Acceptance

- Newly mounted city models use the existing avatar/agent upward code-cell materialization, including its readiness preparation and Reduced Motion equivalent. Established models and the camera remain mounted when another object arrives.
- Play the existing avatar-materialize cue at actual reveal start through the existing audio service. A simultaneous city batch shares one cue (not dozens of stacked sounds); a subsequent arrival batch gets its own cue. Preserve mute/autoplay policy.
- Remove old rise/scan/white-sphere particles. After assembly, thin transparent terminal-rain ribbons extend vertically from model roofs to the camera-relative sky surface, with texture scrolling downward. Preserve selection, truthful status coloring and prop interaction; decoration never owns pointer hits.
- Use the already-packaged lossless 4096px conversion of the exact supplied `02_terminal_rain.png`, verifying source identity. No new texture copy per object.
- Sparse randomized traveling highlights use logo yellow/red/blue/orange, with long quiet intervals and smooth attack/release, not blinking or strobing. Limit simultaneous highlights across the city. Reduced Motion freezes rain and disables highlights. This is comfort-oriented motion design, not a medical safety guarantee.
- A restrained repository-only lighting adjustment may add cool depth haze and cyan rim lighting while keeping actors readable and retaining the accepted floor artwork and layered sky. No sky replacement or expensive full-scene postprocessing.

### Boundaries

Presentation-only, standard-risk browser change. No dispatch/state authority changes, native harness/provider modifications, collision changes, external networking, Phase20 work, merge/release/publication or retained-environment shutdown. TEST45375 and older operator lanes, profiles and state must not be rebuilt or reset. Private branch/commit/push/PR are authorized; first-hand appearance/listening acceptance remains Aaron's.

### Verification

Focused arrival lifecycle/resource/audio batching and rain geometry/timing tests; affected renderer suite; non-emitting typecheck, lint, renderer and web production build; isolated technical browser render/motion/pixel proof. Existing workflow runs provide separate exact-head CI truth. No full native/four-harness acceptance claim.

Local receipts (final source; full evidence outside the repository):

- 119 renderer tests / 19 files pass; workspace typecheck, root lint, architecture (11 tests), changed-file formatting and diff checks pass. Renderer and isolated production web builds pass. Existing bundle-size advisory remains.
- Initial open/clone/recent production browser journeys all passed (3/3), including batched real audio playback. Their screenshots exposed almost invisible/frozen rain despite those functional passes; that initial visual result is **not** acceptance evidence.
- Diagnosis: the low-contrast sRGB texture needed a linear-light alpha range, and R3F copies uniform descriptors. The final shader updates the mounted material's uniforms directly. A regression test models this descriptor-copy boundary.
- Final isolated real-WebGL rendering: above-roof crop has 2,338 visible code pixels (old cutoff: 55), 2,367 pixels differ between deterministic 0s/6s frames, and all four exact logo-color packets render distinctly. Shader/console error lists are empty. This isolated texture/clock proof is separate from the animated sky.
- Rebuilt final production **open** journey passes (one final retest), including materialization, no overlapping arrival cues, normal/static rain modes and no shader errors; assembly, two animated frames and Reduced Motion screenshots inspected. Earlier clone/recent passes are retained as earlier-version evidence, not silently counted as final-source retests.

No medical photosensitivity guarantee, full-city performance benchmark, native Windows operator verdict, or exact-head hosted CI success is inferred from these local checks. Aaron's visual/listening acceptance remains pending.

### Atmosphere references

User-provided `10-terminal-depth-4k.png` and `05-directory-canyon-4k.png`: dark negative space, cyan vertical detail, depth haze and rare warm highlights. True wet-floor reflections and volumetric lighting are optional future choices, not promised here; avoid pretending a roughness adjustment creates screen-space reflections.
