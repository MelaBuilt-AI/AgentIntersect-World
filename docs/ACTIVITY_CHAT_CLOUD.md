# Compact activity chat clouds

> **September 9 delivery update:** Aaron authorized the cumulative PR, push and conditional merge after CI/readiness are green (`1547254171474993302`). The current delivery/acceptance cutline is `PROJECT_STATUS.md`; dated no-delivery and pending-test statements below are historical records, not current authorization. Retained runtimes, public/release actions and later-phase scopes remain protected.

## Depth and translucency correction — Aaron1547043635252428861

Operator reports chat-cloud appearance PASS and both coding prompts, website changes, reports, agent movement and animations PASS. Screenshot `img_9dca98dace6a.png` shows the cloud incorrectly covering a nearer spatial Workbench. Authorized changes: screen-depth-aware partial cloud occlusion and white fill alpha0.6 only; lettering/bezel remain opaque. Preserve silhouette, sizing, animation, DOM sharpness and all accepted workflows. Standard local rendering fix: use existing screen bindings/camera projection, no new dependencies or native coding dispatch. Focused geometry/render tests, affected production browser proof and separate fresh manual lane; leave45289 and older tests/state untouched. No delivery actions authorized.

## Scope — Aaron Discord 1547013345067008040 (2026-09-08)

- Operator PASS: initial load transition is smooth/perfect; open Code Wheel stays behind focused code review.
- Operator correction: DOM bubble text is crisp but the rectangle is too large and text is not centered. Replace it with a smaller floating white cloud, centered black Consolas text, and an embossed streaming-rain bezel matching screens/wheel. Preserve DOM text (including emojis), allow bounded natural expansion, and use gentle hover motion with Reduced Motion support.
- Routine presentation-only change, supported existing local browser renderer paths. No new runtime dependency, WebGL text texture, per-frame React state, video, or full-size rain decode for bubbles. Derive a small reusable tile from the existing rain art; use CSS transforms and cached element dimensions.
- Preserve input passthrough, hidden/behind-camera behavior, viewport containment, cleanup, all accepted scene/entry/focus behavior and safe textContent rendering.
- Do not change/restart/rebuild any existing operator lane, dispatch native coding, alter files in the user's website, reset history, create a replacement Workstream, or stage/push/publish. Build and browser proof use separate output/state.

## Coding-test interpretation

Aaron reports sending the heading task directly through chat without creating a Workstream, observing Codex movement/coding/completion sound, and receiving a one-test-passed completion reply. This is a user-reported plain-chat result, not independently verified file/test evidence or acceptance of Workstream review/preview. Inspect routing read-only; do not rerun or undo that turn. Workstream review/preview retest remains pending.

## Verification — complete local implementation, manual acceptance pending

- Both standard/imported renderer paths retain safe DOM textContent, black 14px Consolas, centered natural content layout, ResizeObserver-cached dimensions, viewport clamping, behind-camera hiding and cleanup. No per-frame layout reads, React state, independent animation timer, new GPU text resource or runtime dependency.
- White cloud face with streaming-rain bezel and static embossed highlight/shade. CSS transforms move the small rain tile and gently float the inner shell between +2/-2px over4.8s; projection owns the outer transform. Hidden clouds pause; Reduced Motion stops float/rain. Width cap260px; long status is bounded to four lines, full authoritative activity remains in existing chat/Inspector.
- Rain art: center1024px crop `(1536,1536,2560,2560)` of existing4096px `02_terminal_rain.webp`, Lanczos resized256px, brightness2.5 (wheel treatment), WebP quality82/method6. Result `apps/web/public/assets/code-world/activity-cloud-rain.webp`, **17,676 bytes**, one shared256×256 decode rather than the7,071,446-byte original per bubble. No artwork changes to screens/wheel/sky.
- Renderer suite **93/93 PASS**; added behind-camera assertion focused2/2 PASS. Startup/lazy-chunk regression1/1 PASS. Renderer build, root/web types, production build, affected ESLint/Prettier and diff check PASS.
- Final production World journey **1/1 PASS**, no skipped/flaky tests/errors, in `attempt-3`; includes ordinary real keyboard travel to a distant agent, source/report/continuation lifecycle, centered short/long/emoji samples, non-clipping text, fixed14px lettering, served tile budget, motion samples and Reduced Motion hold. Browser fixtures are not live native coding acceptance.
- Measured short status148.67×108px; emoji sample217.95×108px; longer message260×129.75px. Direct final full-scene pixels inspected. Crop comparison preserves actual render scale, not a mockup.
- Historical attempt-1 passed but bezel was visually too understated; strengthened rim/highlight, rebuilt. Attempt-2 hit DOMRect floating-point excess260.000061 at the260px cap; comparison now tolerates one CSS layout unit without altering product sizing. Final attempt-3 passed unchanged product. Invalid initial CLI path and temporary config single-file-tsc warning were harness setup, not product failures.
- Evidence/build root: `/home/mela_ai/.hermes/runs/aiw-activity-cloud-20260908/`; `final-verification.json`, `final-build.log`, `attempt-3/browser-results.json`, final screenshots and `cloud-comparison.png`.
- Shared historical pointer-lock screenshot restored byte-identically. Proof frontend/backend listeners45173/43770 exited; all six operator World/site URL checks returned200. Existing operator pages/state and website untouched. No commit/push or activation of the cloud over45287.

## Gentle silhouette follow-up — Aaron1547034620976173199

The initial cloud's deep puff notches crowded expanded text despite passing rectangular containment checks. Changed only the shared SVG silhouette to shallow, smoothly joined curves. Text, layout/padding/size caps, rain tile, bezel and motion are unchanged; more usable white interior instead of shrinking text or enlarging the bubble.

Fresh production build, format/diff checks and affected World browser journey1/1 PASS. Short148.67×108px, expanded260×129.75px; centering, long text/emojis, rain/float and Reduced Motion checks pass. Final short/expanded full-scene screenshots inspected. Evidence `/home/mela_ai/.hermes/runs/aiw-activity-cloud-20260908/gentle-shape/` (build.log, proof/browser-results.json, cloud-comparison.png). Reused prior unchanged renderer/type evidence; no broad rerun. Shared screenshot restored byte-identically and proof listeners exited. Existing operator lanes untouched; manual visual acceptance/activation remains pending.

## Manual test activated — Aaron1547036196104962069

Aaron accepted the gentle screenshot appearance (“Much better!”) and requested a test. Fresh World http://127.0.0.1:45289/ and backend43889 now run independently in tmux `aiw-cloud-20260908`, serving the already-verified gentle-shape build. Native Edge/CDP49289 is at untouched Create Avatar; enabled/visible unowned main window, empty browser storage, exact page and non-overflowing entry screenshot verified read-only. Actual served HTML/CSS/renderer/rain tile matched build; Codex capability available. Separate website seed at `/home/mela_ai/.hermes/runs/aiw-cloud-manual-20260908/veggie-cloud-test` passed1/1, clean checkpoint3f0458b/no remote; no Workstream/native history imported. Prior lanes unchanged. Runtime/readiness/browser records and tested read-only checker in new root. No repeated code gates, native dispatch, setup clicks, consent or preview approval. Live/manual verdict remains pending.

## Depth/translucency implementation and fresh test — complete

- Both renderer variants pass current screen bindings to the shared DOM cloud. Camera-space screen polygons are clipped to the agent depth and near plane; projected nearer pixels form a unioned luminance mask. Docked/back-facing/fully distant/non-overlapping screens do not occlude; movement, yaw and reveal use current poses. Only overlapping cloud pixels disappear. No new dependency, per-frame layout read, React update, timer, raycast or large texture.
- White face `rgb(255 255 255 / 60%)`. Outer highlight and rain layers are true hollow rings (mask-composite exclude), not opaque underlays beneath the translucent face. Content/bezel opacity1; accepted shape, font, dimensions and motion unchanged. Removed face/whole-shell shadows that would darken the transparent interior; embossed outer gradient remains.
- Renderer suite **97/97 PASS**, startup gate1/1, renderer build/root and web no-emit types, affected lint/format and diff checks PASS. Final affected production browser journey **1/1 PASS** in `/home/mela_ai/.hermes/runs/aiw-cloud-depth-20260908/attempt-5/`: actual spatial toggle and footer keyboard movement prove partial nearer-screen occlusion, overlapping farther-screen cloud priority, and cleared mask on HUD return. Short/expanded styles and temporal/Reduced Motion checks retained. Final near/far pixels inspected; tests use fixtures, not native coding acceptance.
- Historical harness issues: accidentally running web tsc without --noEmit created adjacent JS and changed Vite resolution; removed only compiler-byte-matched untracked outputs, rebuilt, startup gate passed. Initial browser assertion needed the actual two-layer computed `exclude, exclude`; text-size probes raced native activity polling, so now use a clearly labeled isolated clone of real DOM/styles while depth proof uses the original. Attempts3/4 passed; attempt5 adds explicit overlap geometry in reverse case. No product assertions weakened.
- New manual World **http://127.0.0.1:45291/**, backend43891, tmux `aiw-cloud-depth-20260908`, root `/home/mela_ai/.hermes/runs/aiw-cloud-depth-manual-20260908`. Matching served HTML/entry/CSS, health/proxy, Codex capability, native enabled/visible unowned Edge and fresh entry/storage verified. Disposable website copied from checkpoint3f0458b into no-remote checkpoint790f087; seed test1/1 PASS. No old Workstream/native history imported. User setup/coding/approval untouched. All old lanes preserved; new manual verdict pending. Keep running, no product commit/push/publication.

## Next operator step

Use the fresh45291 depth/translucency test. For Workstream review/preview, use Repository controls → New Workstream, enter the first task in its task field and start once. Follow `/home/mela_ai/.hermes/runs/aiw-cloud-depth-manual-20260908/README.md`, then report the two changed cloud criteria; prior two-prompt workflow is operator PASS. Do not automatically rerun the already-completed older plain-chat task or claim this fresh seed restored a Workstream. Saved-session recovery, Hermes identity, hosted/multi-agent/Phase20 and delivery gates remain separate.
