# World menu continuity correction

> **Delivery update — September 20:** Aaron1551395372600008805 accepts this cumulative PR slice and authorizes commit/push, then merge PR20 only after exact-head CI and readiness pass. Earlier LOCAL/UNCOMMITTED, pending-verdict, no-delivery and runtime statements below describe historical stages, not current authorization. Final commit/CI/merge receipts are recorded on PR20 and in the session handoff. TEST45399 is retained; PR19, releases, signing and public visibility remain separate.

## Scope and operator verdict

Aaron (Discord1551016868700094485) reports the deployed website substantially better on mobile, improved World performance with all effects enabled, and successful Claude/Codex agent selection. Preserve these scoped passes. Remaining defects: normal Change Avatar uses legacy editor; Change Agent cannot return to the previous World and clears loaded repository/work.

Acceptance:

- Both user and connected-agent Change Avatar use the same minimal, image-choice/name/3D-preview selector as onboarding; explicit save only, cancellation preserves accepted appearance.
- Back to World is available in the Escape menu during in-World selection/setup and returns to the existing mounted World, not fresh entry.
- Change Agent, Add Agent and Change Avatar retain repository generation, World/canvas/camera, chat/draft, current work and preview identity. Existing work stays bound to its actual agent/native session; choosing another agent does not cancel or transfer it.
- Change Agent in Single Agent mode replaces the active agent without promoting to multi-agent; Add Agent separately appends/promotes up to four simultaneous agents. Previously used agents retain their conversations/work and can be selected again within the mounted World. No implicit native-session deletion or work transfer.
- Logout and explicit Reset retain their separate existing semantics.

Implementation/test lane: `/home/mela_ai/AgentIntersect-World-menu`, branch `fix/world-menu-continuity`, based on PR20/78341fa. Do not rebuild or mutate retained TEST45383 or older lanes. Direct implementation with focused RED/GREEN, relevant type/build/unit checks and isolated production-browser proof. No World merge, release, provider/profile changes, website changes or cleanup authorized by this correction.

## September 19 operator retest and authorized correction

Aaron1551059297146896495: minimal selectors and return to the same World/session/repository PASS. Avatar replacement FAIL: user and agents return in T-pose; user recovers only on movement, agent stays stuck. Replay existing materialization on accepted user/agent appearance changes. Change Agent FAIL: clicking another harness merely filters registrations and can appear inert, even after setup. Add Agent itself successfully adds agents.

Current scope: direct correction, focused RED/GREEN and production browser proof, then close only TEST45385/native window with its data preserved, launch a fresh corrected review and prep end session. No Git delivery/CI retry/merge/release, provider/config changes or unrelated cleanup. Keep TEST45385 served bytes stable until authorized replacement.

Acceptance: initialize and visibly advance each replacement's model-local Idle without movement; materialize only the changed body, preserve World/canvas/camera/other actors. Change Agent in single mode replaces the active agent, not implicit multi-agent addition. Configured harness click connects it (explicit choice if multiple registrations); unconfigured harness opens Agent Setup with that section focused and returns to the pending switch after explicit activation. Keep same World/repository/draft/current work/iframe; old native work remains owned, not transferred or cancelled. Add Agent retains its distinct append/promotion behavior. Cancel leaves the active agent unchanged.

## Current manual lane — chat attribution TEST45389

Aaron1551219484042334289 authorized a fresh test and will close his prior browser. New native Edge preview http://127.0.0.1:45389/ is running from the tested attribution build, backend43989/CDP49391, kit `~/.hermes/runs/aiw-chat-review-20260920/`. Fresh setup/state/profile; prior native history is retained on disk, not imported. Read-only health/index/setup-empty/roster-empty/browser-storage/one-page/native-enabled checks PASS, untouched setup pixels inspected. Manual attribution verdict PENDING. Old45387/43987 services owner-checked stopped, data retained; agent did not close old browser. Keep new lane running; no Git/CI/delivery/cleanup actions authorized beyond this replacement.

## September 20 accepted retest and chat-attribution correction

Aaron (Discord1551213646544113725) confirms all previously listed TEST45387 corrections are fixed and working: avatar Idle/materialization, agent replacement/setup, Add Agent and retained World/repository/work/preview flow. Preserve that first-hand PASS. One newly reported defect remains: retained single-agent replies are relabeled with the newest selected agent (Codex → Claude → Beans).

Bounded scope: snapshot each single-turn speaker at dispatch, retain that label through deltas/final/completion and later switches, attach the owning identity when restoring a single-session history, and identify locally generated acknowledgements as World. Unattributed legacy rows must not claim the currently selected agent. Keep transcript order/text, native routing and ownership, prior accepted behavior and all delivery gates unchanged. No cross-provider conversation transfer or new history-persistence mechanism. Standard bug-fix verification: focused reducer/render RED→GREEN, affected tests/type/lint/build, isolated production-browser switch/restore proof. Keep TEST45387's copied frontend/browser/state untouched; no replacement/cleanup, commit/push/CI retry/merge/release authorized.

The original regression reproduced Codex and Claude appearing as Beans. Correction complete LOCAL/UNCOMMITTED:92 affected tests/6 files PASS, web non-emitting typecheck and focused lint/format PASS, normal Vite production build PASS (existing large-chunk warning), git diff --check PASS. The expanded four-harness browser journey PASS60.8s: chat after each switch, original headings retained, exact-session return, refresh and subsequent switch preserve restored Codex attribution; no page errors. Native sessions/API are simulated; this is real production-UI proof, not owner retest or live-provider acceptance. Screenshot inspected: Claw reply retains Claw while Codex is active, legible/no obvious clipping.

Evidence: `/home/mela_ai/.hermes/runs/aiw-chat-attribution-20260920/` contains actual REDs, unit/build logs, failed initial browser trace and successful second report/screenshot. First browser attempt hit the generic5s menu-close deadline; captured page already showed Claude active with Codex history correctly labeled. Test now waits for the identity-bound new-recipient input (30s bound) before checking closure. No production change was made for that timeout. Temporary proof43770/45173 listeners absent. TEST45387 retains its pre-attribution copied UI and user history, untouched; focused owner retest requires separately authorized preview update/replacement.

## Current corrected review — TEST45387

- Prior avatar/switching correction is manually ACCEPTED (September20). New attribution correction is technically green locally, not installed in this retained preview. New URL http://127.0.0.1:45387/ ; backend43987; native Edge49389. Kit `/home/mela_ai/.hermes/runs/aiw-menu-swap-review-20260919/` has README/operator checklist, source/build provenance, actual verification artifacts, readiness/ownership/checker/stopper and native entry observation. Keep it live across handoff with no reset/reload/rebuild.
- T-pose root cause: old passive mixer cleanup ran after new layout initialization and stopped the replacement action through a shared ref. Layout cleanup now precedes replacement initialization. Appearance-scoped readiness/materialization wraps changed user/agent bodies, including changing back, without replacing World/canvas/camera/other actors. Cold user-model loading is separately suspended.
- Configured harness clicks connect directly (multiple identities retain explicit choice); missing/unready configuration opens focused Agent Setup. Successful explicit activation resumes pending switch. Single-agent replacement remains single; Add Agent preserves its separate append/promotion path. Cached choices reuse exact native sessions/avatars when switching back within the mounted World. Existing work/preview stay with original ownership; continuing old work requires its agent, not silent reassignment. Switching waits for current/queued chat turns. No hidden native coding or configuration changes.
- Saved single-agent restoration now selects the actual adapter and correct transcript authority, rather than Hermes-only restoration. Exact-session consent checks remain.
- Full core PASS1343 tests/222 files plus11 architecture checks and format/lint/types/build/smoke; then13 focused production-browser cases PASS. Final cached-return stage full lint/type/build plus204 impacted tests/26 files PASS. Final transcript-restore correction:80 affected tests/7 files PASS, and all-four-harness roundtrip + refresh PASS. Three other switch/retained-work cases passed immediately before that final narrow authority correction. No fresh full-matrix or hosted-CI claim.
- Native Windows Edge proof: two normal-motion tests each swap user/agent away and back; exact new source/action-time/bone progression without movement, same canvas and other mixer. Partial materialization + final native pixels inspected: arms down, grounded complete bodies; unchanged actor intact. Native rendering uses simulated session authority, not provider coding acceptance. Evidence `/home/mela_ai/.hermes/runs/aiw-avatar-swap-native-20260919/`.
- TEST45385 was protected during builds by copying its served web bytes and atomically repointing its existing link; no page reset. After proof, its exact native root31620 and service tree were verified/closed; ports45385/43985/49385 absent, all data retained. Temporary proof49387/root33696 also verified/closed. Unrelated retained World/website lanes untouched.
- New manual launch starts fresh setup/avatar state; no previous roster/native conversations/Workstreams imported. Frozen copied normal frontend, matching existing built backend, harmless no-remote sandbox. Same-origin health/index-byte and empty setup/roster/storage checks passed; one native25796 page and untouched contained setup screenshot inspected. Only the human performs setup/consent/coding. Optional local voice not preinstalled.
- All work remains LOCAL/UNCOMMITTED on78341fa. No World push, CI retry, merge, release, provider changes or Phase20. Next: Aaron's scoped retest; authorized integration/CI/delivery afterward. Existing website/performance passes and paused PR19 release checklist remain unchanged.

## Verification and current delivery state (prior correction, historical)

- Reproduced the legacy change-avatar UI with a failing SSR regression, then passed all17 imported-avatar tests. Browser REDs demonstrated missing minimal user selector and actual World unmount on Change Agent.
- Core gate passed: format, lint, complete typechecks,11 architecture checks,1343 tests/222 files, production build and smoke. Log: `/tmp/aiw-menu-core-corrected.log`. Initial core invocation caught an unused fixture variable; corrected before this PASS.
- Ten affected production-browser journeys passed in4.2m: new continuation regressions, original focus/setup/Add Agent single→multi, role selection/save, legacy migration and Reset/Logout semantics. Output: `/tmp/aiw-menu-final-browser/`.
- Final appearance checks caught/corrected Cancel/hint overlap and the nested Change Agent logo's viewport-based overflow/wordmark overlap. Final CSS built successfully, and all four menu-continuity cases passed again in1.8m in `/tmp/aiw-menu-final-green/`; desktop/portrait pixels inspected. No full-matrix/native-provider acceptance inferred.
- Continuity tests exercise real production UI with explicit simulated native/API fixtures: one repository index, unchanged actual World/canvas nodes, retained agent-owned working Workstream, unchanged actual iframe and unsaved preview-document input, retained chat draft, primary and secondary avatar acceptance, successful Change/Add Agent and cancellation. No work cancel/end/rebind/recreate requests are allowed. This is not a new live-native coding test.
- Early browser harness attempts failed from two ambiguous selectors before reaching the target; corrected selectors then reproduced the real multi-agent avatar-save bug (it incorrectly required an onboarding pending-roster ID). In-World acceptance now resolves the actual connected roster entry and supports each connected agent.
- After verification, Aaron explicitly authorized preview replacement (Discord1551037368646242356). Old TEST45383/43983 and its exact native Edge browser were closed with all state/profile data preserved. Other older lanes and website previews remain untouched.
- Corrected manual preview **http://127.0.0.1:45385/** (backend43985) is running under `aiw-menu-review-20260919.service`; kit/README/checker/stopper in `/home/mela_ai/.hermes/runs/aiw-menu-review-20260919/`. Fresh setup/state/profile; old history not imported. `web` links to this checkout’s tested `apps/web/dist`: do not rebuild beneath the operator. Read-only health/ownership/feature-chunk byte checks and untouched native setup screenshot passed. No app interactions automated. Manual verdict **PENDING**.
- Changes remain **local/uncommitted** in `fix/world-menu-continuity`. Aaron1551045813768032310 will test shortly and requested end-session prep: leave this exact preview/state running. Next: receive his correction verdict, then separately authorize private delivery/PR20 integration and exact-head CI. Earlier performance/agent-selector acceptance remains valid. No new merge, release or provider authorization.
