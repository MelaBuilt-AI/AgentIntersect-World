> **Historical record only.** Superseded by [THREE_WEBGPU_UPGRADE.md](THREE_WEBGPU_UPGRADE.md). Earlier RUNNING, pending, scope and media-origin statements describe their recorded attempts, not current authority. Original narrative follows intact; no old failures are promoted to PASS.

# Three r186 / WebGPU / SunLight upgrade

## Current normal test — TEST45447 RUNNING

**Latest first-hand verdict — distant lightning visually ACCEPTED.** Aaron: “nailed it Fluff! love the distant lightning now!” (Discord `1553070041526829118`). This accepts the new distant horizon-lightning appearance, preserving prior FPS/rock-shadow/existing-lightning acceptance. It does not independently establish other settings, follow/Run, persistence, or delivery gates. TEST45447 stays running; no cleanup, replacement, Git delivery, or release was requested. Earlier horizon-pending wording below is superseded by this scoped verdict.

Aaron authorized replacement in Discord1553062537548005447. The verified horizon-lightning frontend and matching compiled backend are now running at **http://127.0.0.1:45447/** (backend44047, Edge CDP49447, service`aiw-horizon-review-20260925.service`). Fresh setup/profile/state is at`C:\Users\Mela AI\AppData\Local\AgentIntersect-World\acceptance\aiw-horizon-review-20260925`; launch/ownership/cleanup kit`~/.hermes/runs/aiw-horizon-lightning-20260925/review/`. Frontend entry index-D6dVs-1Y.js matches tested bytes; backend uses candidate`server-dist/index.js`, not older source dist. Scoped original FPS/shadows/upper lightning acceptance remains; new horizon appearance/pace pending.

Old TEST45443 services are stopped and their three owned processes/listeners exited; its native browser was already closed. All old profile/state/work roots remain retained. Fresh setup reports zero registrations/roster; one enabled native Edge window, empty storage, Discover Agents enabled, Continue disabled, zero observed page errors. No app clicks, agent attachment, provider activation or user-state import occurred. Optional local voice remains unavailable/not installed, as before. Read-only `check.py` and `stop.py check` plus profile-bound `close-browser.py check` pass. Explicit cleanup only: `close-browser.py stop`, then `stop.py stop`; retain state. Below are historical pre-launch statements where superseded by this current runtime notice.

## September25 additive horizon lightning — verified, not installed in current TEST

Aaron reports FPS much better, decorative rock shadows good to go, and existing lightning visible/working (Discord `1553048044839116852`). Preserve those scoped first-hand PASS results; follow/Run was not mentioned. His annotated image `~/.hermes/cache/images/img_5aff2a324a2f.png` requests smaller, quicker distant flashes just above the mountain silhouettes **in addition to** the accepted higher bolts. The red lines are placement guides, not red lightning artwork.

**The addition is locally TECHNICAL-GREEN. TEST45443 remains RUNNING with its accepted prior build/state untouched; the new horizon layer is NOT installed there yet.** No close/reload/replacement was requested this turn. Separate candidate/evidence root: `~/.hermes/runs/aiw-horizon-lightning-20260925/`; frontend `web-dist/`, matching compiled backend `server-dist/`. Pair a new frontend with the new schema/backend when replacement is authorized; do not proxy it to the older running backend. No Git/provider/publication/installers/Phase20 work.

- Optional `weather.horizonLightning` contains density0–1, interval2–12seconds per region and elevation0–20degrees. Omitted/density0 is off, so existing saved recipes and higher/local lightning remain unchanged. Default explicit settings density0.7/interval4/elevation2; prompts for busy distant storms can select density0.8–1/interval2–3. Elevation controls the lower band independently of the upper sky.
- At most24 spatial regions share the already-uploaded supplied bolt atlas. Per-region phases produce brief0.42second flashes with varied source frames, slant, size and position—not a synchronized fullscreen strobe. Own live UV/opacity nodes prevent shared upper/local atlas matrices from changing this layer or requiring texture copies/reuploads. Soft localized glow follows existing flashes control; Reduced Motion unmounts weather. No new point/shadow lights, ground impacts or thunder-event spam.
- Added independent controls under Weather and expanded scenery → Distant horizon lightning; pinning/removing it preserves upper/local settings. Agent and copied briefs describe the additive semantics and bounds. The generated report names horizon lightning separately, including horizon-only recipes, rather than falsely describing all lightning as off. Save/restore/cycling retains the new fields.
- Native `native1` showed a correctly rendered but too-high lower band when tuned against the cylinder's transparent upper boundary. Final default2degrees was tuned to the **visible** mountain silhouettes. `native2`/`native-final` show small distant bolts immediately above the ridges, spatially local glows and retained larger upper branches. Four matched on/off captures prove visible contribution at progressing times/positions; the actor is retained. Reduced Motion leaves zero weather objects in final native proof. No new full-product FPS, listening or human verdict is inferred.

Verification: `gate.log` PASS —60files/236affected tests,11architecture, scoped formatting/lint, schema/renderer builds, web/backend types, normal frontend build and isolated backend compilation. Isolated real component → preview → Custom2 disk save → reload/cycling passes with horizon settings; API persistence across new instances passes. `smoke.json` runs the newly compiled full backend twice with disposable state: real HTTP slot save/read after restart preserves both upper/local and horizon settings; owned listener closed. `native-final/report.json` has zero captured errors, visible lower/upper layers, temporal samples, Reduced Motion removal and retained actor. `summary.json` aggregates results. Entry`index-D6dVs-1Y.js`404614bytes below unchanged409600ceiling; sha256`f09fa0ecaa8f8369afe1a0062188bb99ebae56173de1e203f28d63a0ef4c1f2f`. Existing Three CJS/optional large-chunk warnings remain.

Current accepted review ownership re-attested unchanged:45443/44043, service`aiw-sky-cutout-follow-20260925.service`, supervisor4101095/backend4101102/frontend4101103, Edge31276/CDP49443, served index digest unchanged. Its setup/state/browser were not reset or navigated. New proof45445/CDP49445 and smoke listener are closed. All old work/profile/state and protected Hermes8642/OpenClaw18789 retained. Source237paths(59tracked/178untracked),zero staged, unchanged basef3755fd onfeat/hack-your-world.

Next: authorize replacement or continue the retained test; then Aaron reviews the new lower band's density/pace/elevation. Prior follow/Run and unrelated manual/media/release caveats remain scoped as before. **All candidate/runtime statements below are historical where superseded here.**

## September25 cutout / sky-lightning / follow / effects corrections — TECHNICAL GREEN

Aaron accepts the previous transition appearance and reports much better running performance; nearby lightning works well (Discord `1553030479660453931`). His requested additions, including two mid-turn requests, are implemented directly. **Fresh TEST http://127.0.0.1:45443/ is RUNNING at untouched Agent Setup; Discover Agents first. LEAVE RUNNING. Old TEST45439 is CLOSED; all profile/state/work retained. New human verdict PENDING.** No Git/provider/publication/installers/Phase20 work.

- **Cutouts:** alpha-tested SunLight silhouettes cast from either light-facing side; original asset bytes remain intact. Catalog/importer alpha bounds position the real image base rather than its transparent padding; a small seat inset and soft contact shade improve grounding. These remain flat, camera-facing decorative pictures, not volumetric rocks/meshes. Native rock/driftwood shadows are irregular, not rectangular; their flat silhouette/contact illusion (especially raised driftwood ends) still has artistic limits and awaits Aaron's verdict. No density/modeling redesign.
- **Sky lightning:** distant events now spread 3–8 bolt/arc pairs around camera-relative sky, selected by existing intensity, with soft localized flashes; local/both/off semantics, existing cadence, impacts, audio ownership and Reduced Motion remain. Automatic and copied generator instructions explicitly map skybox requests to distant, mixed sky/nearby to both, honor exclusions, and explain that static clouds/thunder ambience do not create lightning. Settings label sky-wide mode clearly. No new live-provider dispatch was required; earlier provider evidence stays scoped.
- **Actual atlas fix:** native still-camera proof exposed r186's ordinary-material observer ignoring UV-only changes while texture version/object transforms stayed stable. The draw could retain the blank first atlas frame even as CPU offset/matrix advanced. Explicit live node-material atlas sampling now updates without reuploading images; shared maps/materials are disposed by their owner. Native captures show sharp branching sky bolts, later animated atlas states, real sky glows and preserved local strikes. A CPU-only matrix correction was insufficient and is retained in failed/intermediate evidence.
- **Follow Run:** distance already selected Run but translation still used the walking request speed. Follow now uses at least8world units/second beyond the existing catch-up threshold, returning to request walk speed near the user. Explicit coordinate/repository speeds are unchanged. Regression proves0.8 versus0.4unit displacement per0.1s at default follow speed, then Walk/rest without overshoot or losing the follow intent. New normal-product human retest pending.
- **All-effects optimization:** actual native Default/48-city-model profiling found a redundant pair of SunLight cascade draws inside fog's depth/alpha-only capture. Legacy renderer.shadowMap.autoUpdate=false did not suppress modern SunLight. Hide/restore lights only during that unused-color capture; keep visible shadow cascades, reflection atlas, fog, bloom, AA, rain, shafts and original resolutions unchanged. Matched same-scene/time legacy-capture versus correction:592→520draws and22→20passes; compared images differ by at most1channel value (same-mode repeats also differ by1). Timing samples are mixed, so no native/whole-product FPS percentage is claimed. Initial read-only operator sample happened after he switched to Sunlit Trails (119.75RAF/s at1912×948), not a reproduction of his earlier Original/Clone-repo slowdown. The isolated Default comparison, not that operator sample, grounds the optimization.

**Verification:** `~/.hermes/runs/aiw-sky-cutout-follow-20260925/` owns all evidence. `gate.log`:57files/232affected tests,11architecture, formatting/lint, renderer/schema builds, web/backend types, separate normal web build and backend compilation PASS. Subsequent normal backend rebuild is byte-identical across60JSfiles to tested isolated compilation; conventional disposable startup/API smoke PASS. Entry`index-Cxkz_0yx.js`404614bytes under unchanged409600ceiling; actual served JS/CSS match tested web-dist. Native `live-atlas/`, `contact-final/`, `fog-optimized/` record zero captured errors. Historical probe syntax/signature failures and initially invisible atlas attempts remain separate, never promoted to final visible proof. Existing Three CJS and optional large-chunk warnings remain. The lint invocation also notes one ignored Markdown file; source lint has no errors and Markdown was formatted.

**Current runtime:** kit`~/.hermes/runs/aiw-sky-cutout-follow-20260925/review/`, service`aiw-sky-cutout-follow-20260925.service`, frontend45443/backend44043/CDP49443; root`acceptance/aiw-sky-cutout-follow-20260925`. Supervisor4101095/backend4101102/frontend4101103; native Edge31276/creation1790344735542, enabled ownerless window3672804; WSL browser wrapper`proc_168d0f1f9877`/4103320. Current read-only owner helpers pass. New launch completed=false/registrations=[], browser storage empty, one intended page, correct entry, no clipping/modal/errors, no app clicks/typing/consent. Do not repeat launch-only empty-state assertions after user use. Optional voice remains user-controlled/not-installed. Survives chat, not guaranteed host shutdown/sleep.

**Retention / next:** old Edge4208 and exact45439/44039control-group were re-attested/closed, all10non-browser state/work hashes unchanged; profile retained, no deletion. All isolated proof browsers/services closed; protected Hermes8642/PID721 and OpenClaw18789/PID722 unchanged. Source233paths(59tracked/174untracked),zero staged, unchanged base`f3755fdb55330ce255b07ea816e6b74181ad56c4` on`feat/hack-your-world`. New manual review is next; no blanket acceptance of historical HUD/audio/weather/voice/recovery criteria. Media/public-rights, true3Dprops/physicalweather, native Windows backend package, Git/PR26 delivery, installers/release and Phase20 retain their separate gates.

Read-only from current kit: `python3 check.py`, `python3 stop.py check`, `python3 close-browser.py check`. Stop only on new explicit request, using current browser helper `stop`, then service helper `stop`; all data stays retained. Do not rebuild served web-dist or reset/reload the user's browser. **All runtime/acceptance statements below are historical where superseded above.**

## September25 operator feedback — follow-up authorized

Aaron accepts the transition appearance and reports the combined build runs much better (Discord `1553030479660453931`). Nearby lightning works well. New requested corrections: alpha-cutout prop shadows/grounding, multiple distant skybox bolts/arcs/flashes, and (mid-turn) Run translation speed while following. He additionally requests a default-World/Clone-repo all-effects optimization with effects active and visually unchanged. Implementation and affected verification in progress; TEST45439 remains untouched until the verified replacement is ready. Closure followed by a fresh test is authorized; all old state/work must remain. No Git/provider/publication/Phase20 authority. These scoped verdicts supersede the blanket pending label below, not unrelated manual criteria.

## Shadow/switch correction and transition polish — locally verified

Aaron supplied `C:\Users\Mela AI\Videos\shadows and stutter.mp4` (Discord `1552875728255590413`), SHA256 `9d1baa2c661ffc1a60e6693567f46e6fc6278c70f2d2c3393849eb185a185522`, then requested faster avatar-style dematerialization/materialization after those corrections. Both changes are implemented and technically verified. **Fresh combined TEST http://127.0.0.1:45439/ is RUNNING for Aaron at Agent Setup; LEAVE RUNNING. Prior TEST45433 is CLOSED with profile/state/work retained. Combined manual verdict PENDING.**

### Current operator review — September25

Aaron explicitly requested old-test closure, a completely fresh test left open, then end-session prep. Exact old Edge6204 and `aiw-webgpu-corrected-20260924.service`45433/44033 were re-attested and stopped; eight state files hash-recorded and all work/profile/native history retained. New kit`~/.hermes/runs/aiw-shadow-switch-20260924/review/`, unit`aiw-world-polish-20260925.service`, frontend45439/backend44039/CDP49439, new root`acceptance/aiw-world-polish-20260925`. Supervisor2682833/backend2682838/frontend2682839; Edge4208/creation1790308959250/ownerless enabled HWND3278732; wrapper`proc_28ab72cac19b`/2684553. Healthy matching backend/proxy, exact served JS/CSS and all read-only owner helpers pass. Untouched launch Agent Setup: empty registrations/local/session storage, Discover Agents enabled, Continue disabled, screenshot inspected; zero app interactions. Local voice remains not-installed/user-controlled. Do not rerun empty-state assertions after operator use. No rebuild/reset; service survives chat turns, not host shutdown/sleep.

### Corrections

- **Detached shadows:** the wet-floor reflector refitted the main SunLight's single cascade atlas/matrices to its mirror camera during the main pass. A retained reflector-owned SunLight and separate atlas now preserve main-view shadow state while still rendering real reflections at unchanged resolution. Neutral-floor native on/off captures reproduce the original detached patch and verify attached silhouettes with reflections enabled after correction; main matrices now match with reflection on/off.
- **Repeated city rebuilds:** one persistent lighting/fog rig updates existing values rather than replacing light identities across Original/scenery. Original's hidden reflector and interference nodes remain mounted. The city, camera and provider state are not replaced.
- **Cold scenery preparation:** one hidden replacement is mounted and its actual materials are asynchronously compiled before the visible swap, then GPU completion is awaited through the real private compositor pass. A narrowly scoped r186 context adapter fixes `compileAsync` selecting call-depth zero rather than the compositor's depth-one context; the temporary adapter/target/visibility are restored synchronously before awaiting. This pinned compatibility seam needs revalidation on engine upgrades. Old scenery is excluded only from the private preparation draw, never from the operator's displayed frame.
- **Image stalls:** native CPU profiling confirmed synchronous HTML-image conversion during upload, including `createImageBitmap(HTMLImageElement)`. Decode from cached response blobs instead; close owned bitmaps on disposal/cancellation. The grass coverage lookup also avoids forcing a full-image decode in its tiny canvas read. Original and staged scenery share one already-prepared transition-rain texture, avoiding another first-use upload. No asset, DPR, shadow resolution, shader-target resolution or detail reduction.
- **Transition polish:** reuse the avatar's actual streaming-cell arrival shader for environment-only dematerialization/materialization. Preserve the existing 350ms outgoing / 650ms incoming pace and flicker. Distribute cell thresholds across floor/sky instead of a broad horizontal sky wipe; fade the separate floor grid too. Old environment reaches fully absent before the new assembly; actors/city remain untouched. Reduced Motion bypasses the animated dissolve. Existing finite floor edges and city-owned terminal rain are not new transition defects.

### Evidence and remaining scope

Root: `/home/mela_ai/.hermes/runs/aiw-shadow-switch-20260924/`.

- Final native evidence: `shared-rain-final/render-report.json`; zero captured errors, WebGPU r186, 12 real city models plus a clearly labeled reference cube. Six built-in switches through Sunlit/Mars/Original, then eight held dissolve/assembly samples. Reference actor identity/visibility retained; main shadow matrices are invariant under the reflection toggle. Held samples establish spatial composition, not realtime pacing acceptance. Earlier strip-like reveal/grid-leftover captures remain under `combined-final`; corrected distributed/grid-cleared captures follow.
- Matched fixture peak frame intervals, baseline → final: **360.4→97.3, 346.3→31.5, 365.9→44.4, 149.8→37.3, 142.1→29.5, 166.9→18.3ms**. Final candidate texture uploads peak2.1ms versus individual HTML uploads around100ms. First-use latency is reduced, not eliminated; no zero-stutter, full-product FPS, 4K operator-workload or all-device certification is claimed.
- `shared-rain-final-gate.log`: final scoped formatting/lint, renderer build/types, web types, **42files/169tests** (renderer, switcher, candidate startup-boundary), isolated production build and disposable startup/API smoke PASS. Conventional API smoke uses the unchanged backend/normal app-dist scope; the startup-boundary test and separate web build exercise the candidate. Prior whole-project1507test proof remains historical, not a new full run. Existing Three CJS and large-chunk warnings remain.
- Separate normal production candidate: `web-dist/`, entry`index-D4CURoq7.js`,404614bytes below409600ceiling; SHA256`4aed65877dfcf877f6dbaad30a75054af94e48803f6a63a27152d0dc58b67e25`. `final-summary.json` aggregates actual results. TEST45433's served entry remains byte-identical to its prior receipt.
- Preserve invalid fixture attempts: reserved`window.status`, nonexistent fourth preset, and profiler variable-shadowing failures; none is a product failure or PASS. A final-gate attempt stopped on unused test callback parameters; corrected and rerun. All native proof browsers and services are closed, with zero matching native processes/CDP listeners and no WSL proof listener. The operator test was not refreshed, navigated, reset or stopped.
- Source228changedpaths/0staged at unchangedbase`f3755fdb55330ce255b07ea816e6b74181ad56c4`; no Git delivery/hostedCI, provider changes, releases/installers/publication or Phase20. Next: Aaron reviews the running fresh TEST45439; full native motion/comfort acceptance remains his.

Earlier sky-only candidate and acceptance language below are historical; the combined candidate above is current.

## Default sky scale follow-up — September 24

Aaron reports the corrected build is working well; the only reported concern is Original's finer code-sky scale, while the scenic skyboxes look crisp (Discord `1552872306685120514`). Preserve that first-hand verdict for the tested build without inferring every historical checklist item was separately exercised. He authorized the default-only correction in `1552873427205890129`.

**Default-sky correction is locally verified, not yet installed in TEST45433.** The existing test remains running with its prior served bytes and retained work/state. A separate production build is available at `/home/mela_ai/.hermes/runs/aiw-default-sky-scale-20260924/web-dist/`; no automatic browser refresh, reset or replacement occurred.

- Cause: `codeSkyMaterial` manually repeated UVs 6×3; r186 `TextureNode.sample()` cloned its null-UV source without preserving `updateMatrix=false`, reapplying texture repeat for effective 36×9 density. Reproduced with actual material inputs and the installed dependency.
- Correction: explicit sampling UVs prevent the second transform. Original-only density is 8×4: larger glyphs than the faulty build, modestly finer than the old 6×3 scale. Removed the redundant Original sky texture-repeat assignment. Scenic mapping/materials, shared loaders, floor, camera, lighting and animation formulas are unchanged.
- Focused sampling regressions failed for all three layers before the fix and passed afterward. Complete renderer plus startup-boundary run: **35 files / 157 tests PASS**. Changed-file formatting/lint, renderer typecheck/build, isolated production web build and disposable startup/API smoke pass. Startup-boundary proof builds the candidate; the conventional smoke retains its existing unchanged app-dist/backend scope. Existing Three CJS and optional chunk warnings remain.
- Native Windows Edge WebGPU r186 matched-camera fixture: faulty scale, old-scale reference, candidate and candidate-at-3s captures; final zero captured errors. Final stills independently inspected: requested intermediate scale is visible, no white sky/floor. Lower floor-region pixels are identical; temporal sky samples differ. This is isolated rendered evidence, not a normal-product journey or new human appearance/comfort verdict.
- Evidence: `/home/mela_ai/.hermes/runs/aiw-default-sky-scale-20260924/`. Initial proof failed strict monitoring on Vite dependency reoptimization 504 plus a missing fixture favicon; retained under `attempt1-vite-startup-failure/`. Fixed fixture favicon and reran with stabilized dependencies; no product change or error suppression for that failure. Final proof browser/server closed; native process/CDP and WSL listener checks confirm cleanup.
- No Git delivery, provider changes, releases/installers/publication or Phase20 work. The earlier TEST45433 pending verdict/untouched-entry observations below are historical; its runtime remains retained, but Aaron has now used it.

## Prior operator-reported rendering/stall correction — September 24

**Correction locally TECHNICAL-GREEN. Fresh normal TEST http://127.0.0.1:45433/ is RUNNING at untouched Agent Setup. LEAVE RUNNING for Aaron. Previous TEST45429 is CLOSED, its state/profile/work retained. Corrected-build manual verdict PENDING.**

Aaron's report (Discord `1552814512111886386`) is authoritative FAIL for intermittent white Original floor/sky on cold load or after cycling, dark avatars, first materialization pauses, city shadow delays/stalls and the first upward-stream pause. Supplied screenshots and `C:\Users\Mela AI\Videos\issues.mp4` are preserved. Video SHA256 `993230a1b7e1b063086a342cd2b1eda55edb7381f9fca08ee5be9fe912cbf233`; contact sheet and freeze-detector output are evidence aids, not a substitute for his temporal verdict.

### Corrections and causal evidence

- **Texture ownership:** the transition converted the floor while its map was null, leaving React to update a detached material. On return, cleanup restored obsolete white sky materials. Delayed floor/aurora/nebula versus rain loading reproduced both actual pixels and mismatched material UUIDs. Original floor and sky now keep stable React-owned node materials; map/node updates happen in place and cleanup cannot overwrite a newer owner. The same native load-order and return regression passes.
- **Readable avatars:** actual imported-model shadow on/off comparison did not materially restore detail; added indirect fill did. Original ambient fill is 1.8, matching the existing preview strength; scenic hemisphere fill is recipe ambient ×1.5, preserving palette and relative day/night choices. Sun direction, shadows, asset materials and textures remain unchanged.
- **Real GPU preparation:** r186 skipped hidden groups, so baseline precompile calls prepared zero objects. Merely exposing the group or matching scratch target format was insufficient: r186 render-context keys include nested call depth. Preparation now batches bodies through the actual compositor/reflection/shadow path into a private output target, restores visibility/target synchronously, and never presents that warmup frame. Originals are prepared before arrival clones; existing delay, motion, sound and cleanup stay intact. Redundant depth-zero compilation is omitted with the compositor active.
- **City first-use effects:** final rain/shaft/fog/spark components now mount hidden before city readiness. Their clocks/cues stay inactive until settlement; model and effect preparation jointly gate reveal. The same four-real-asset fixture changed from 540.9ms at settlement/first stream to 14.3ms maximum afterward, with zero late effect pipeline creations. This is an isolated native GPU measurement, not whole-product FPS or human smoothness acceptance.
- No DPR, model detail, shader resolution, animation amplitude, SunLight cascade resolution, or enabled Graphics effects were reduced. No scripted product-navigation journeys or provider reruns.

### Current correction evidence

Root: `/home/mela_ai/.hermes/runs/aiw-webgpu-corrections-20260924/`.

- `core-gate.log`: consolidated PASS, 256 files / 1507 tests plus 11 architecture, formatting/lint/types/build/startup/API. This precedes the final GPU-completion guard; do not call it a full 1508-test run.
- `queue-renderer-tests.log`: current source 33 files / 153 tests PASS, including RED→GREEN GPU-completion readiness. `final-affected-gate.log`: final formatting/lint/types/build/startup/API PASS after that guard. No repeat of unrelated full tests was needed.
- `render/red`: native WHITE floor/sky reproduction and mismatched drawn/declarative material identities. One Vite initial dependency-reoptimization 504 is retained separately; it is not the texture cause.
- `render/texture-green`, `lighting-diagnostic`, `warmup-baseline`, `visibility-green`, `formats-green`, `pipeline-diagnostic-3`, `cache-diagnostic`, `retained-targets`, `real-pass-green`, `city-baseline`, `city-green`: causal progression. Visibility-only and matching-format preparations were incomplete experiments, not accepted fixes. `pipeline-diagnostic` used the wrong diagnostic property (`renderContext` versus `context`), flooded errors and timed out; its exact owned proof browser was subsequently closed. No operator browser was used for these fixtures.
- `render/final-city`: three delayed-map cold reload→scenery→Original repetitions pass with drawn/declarative UUID parity. `render/graphics-off`: no-compositor imported arrival completes, no errors.
- Final `render/queue-city` and `final-summary.json`: native WebGPU, zero errors; 13.4ms maximum avatar waiting/assembly and 13ms city completion/first-stream sample, no late avatar or stream-effect pipeline creation. CPU submission alone initially released readiness before GPU completion; the last guard awaits the actual submitted-work promise. Preparatory CPU/GPU work still exists before the existing loading barrier releases. These are scoped fixtures, not zero-latency loading or a device-wide FPS claim. One earlier aggregate wrongly required zero pipeline creation of any kind at city settlement; preserve `summary-pre-queue-history.json`, not that overbroad assertion, as history.
- Corrected Original floor/sky/cat and four real city models/streams were image-inspected. The gold cube/reflection belongs to the isolated fixture, not the product. Native fresh-entry screenshot also inspected with no clipping/modal/blank defect.

### Runtime and boundaries

Old review root `acceptance/aiw-webgpu-review-20260924` is retained, with eight state-file hashes in `failed-state-hashes.json`. Exact Edge14476 and service `aiw-webgpu-review-20260924.service` were owner-verified and closed before rebuilding the served dist. Ports45429/44029 are absent. Hermes8642/PID721 and OpenClaw18789/PID722 unchanged.

Fresh replacement kit: `~/.hermes/runs/aiw-webgpu-corrections-20260924/review/`; frontend45433/backend44033/CDP49433 on `aiw-webgpu-corrected-20260924.service`. New root `/mnt/c/Users/Mela AI/AppData/Local/AgentIntersect-World/acceptance/aiw-webgpu-corrected-20260924`; supervisor2156683/backend2156690/frontend2156691, Edge6204/creation1790293489814/window3737864, wrapper`proc_e430ca98e3fd`/2157760. Re-attest identities before any stop. No old roster/browser state imported; retained projects/native histories stay on disk. Fresh setup completed=false/registrations=[], local/session storage empty, one enabled native window at Agent Setup; Discover Agents enabled, Continue disabled. Optional voice not-installed, setup/consent operator-owned.

Actual normal served entry `index-VBJi5siP.js`,404614bytes (unchanged409600ceiling),SHA256`f29e3ee8514051e3bfbccd8e6cb2726036998792ddb86ffeb90ceb6546cc6fa6`; JS/CSSbyte-match, same-origin proxy/backend health and read-only owner helpers pass. Leave state/browser/build untouched for review; Windows/WSL shutdown or sleep can stop it. Read-only: `python3 <kit>/check.py`, `python3 <kit>/stop.py check`, `python3 <kit>/close-browser.py check`. Closure only when separately requested: browser helper `stop`, then service helper `stop`; retained data is not deleted.

Source is still `feat/hack-your-world` at base `f3755fdb55330ce255b07ea816e6b74181ad56c4`, 221 changed paths (57 tracked / 164 untracked), zero staged. PR26 remains the older hosted checkpoint. No commit/push/ready/merge/hosted CI/publication/releases/installers/download activation/provider or profile changes/Phase20 authorization. Carry-forward human and media-rights caveats below remain open.

## Historical migration record — before this operator correction

All TEST45429 leave-running instructions, counts and verification labels below are historical; the correction section above is authoritative.

### Migration result — September 24, 2026

**September 24 operator retest FAIL; authorized correction in progress (Discord `1552814512111886386`).** Aaron reports intermittent white Original floor/sky on cold load and after environment cycling, dark avatars, consistent first-materialization pauses, delayed/stuttering repository-object shadows and first upward-stream activation. Screenshots and `C:\Users\Mela AI\Videos\issues.mp4` are supplied evidence. Prior technical PASS remains historical, not manual acceptance. Fix and optimize these exact defects, preserve failed TEST45429 state/work, then owner-close its browser/services and launch a fresh corrected normal test before end-session prep. No Git delivery/publication or provider changes authorized.

Aaron authorized Three 0.186.0, WebGPU, SunLight and supported PCF shadows, testing, obsolete TEST cleanup and a fresh review left running (Discord `1552736247494213713`). After a session pause, he requested continuation (`1552756373014712331`). No commit/push/ready/merge, publication, releases/installers/download activation, provider/profile changes, original AgentIntersect work, splats or Phase 20 are implied.

Source: `/home/mela_ai/AgentIntersect-World-menu`, branch `feat/hack-your-world`, base HEAD `f3755fdb55330ce255b07ea816e6b74181ad56c4`. **214 changed paths: 53 tracked / 161 untracked; zero staged.** Includes inherited feature work. PR26 remains an older hosted checkpoint; no new hosted CI or delivery occurred.

Aaron explicitly confirmed he has not tested yet and will report after returning, then requested end-session prep (Discord `1552768904730910761`). Keep the current review running and untouched; encouragement is not a manual PASS.

## Implemented scope

- Pinned `three@0.186.0`, with R3F 9.6.1 retained. All World/preview Canvas owners use awaited modern renderer factories with real backend/revision reporting; previews retain constructor AA.
- Both World canvases explicitly choose supported PCF (`shadows="percentage"`). Registered SunLight/SunLightNode supplies two cascades, 1024 per cascade, bounded far100, bias0/normalBias0.015. Original/scenic directions, colors and intensities retained; new lighting appearance is not user-accepted.
- Offline authored GLSL → static TSL nodes for ground/details/sky, surface rewrite, arrival, weather, rain/fog/sparks. No browser transpiler. Preserve initialized local value copies and actual instance-step attributes.
- Bounded instanced particle quads preserve seeded populations and projected pixel sizing. Rain launch time no longer mutates the city clock.
- Node RenderPipeline/bloom preserves source alpha. Real geometry reflections include five-tap softening. Shared fog capture runs before the main pass with a distinct synchronized camera.
- This continuation formatted and verified the final reflector edit, then fixed a demonstrated repository-rain blackout: `rainColor` is a Three Color, requiring a `color` uniform rather than `vec3`. XYZ serialization had uploaded undefined components, causing NaNs that spread through bloom. The one-line binding correction plus a focused regression preserves the authored appearance/formulas.

## Final verification

Evidence root: `/home/mela_ai/.hermes/runs/aiw-webgpu-upgrade-20260924/`.

- **`core-gate-5.log`: PASS** under explicit Node24.18.0/pnpm11.15.0 and a 3GiB/no-swap scope: formatting, lint, types/package checks, 11 architecture tests, **254 files / 1500 tests**, normal production build and startup/API smoke.
- Actual served entry: `index-qlmiAMML.js`, **404614 bytes**, below unchanged409600-byte ceiling. SHA256 `75824a99b7d9e5b079306bcd55d8c28584fd1d1e3d38adaabad52c01634d5859`. JS and CSS served bytes match current dist. Optional large-chunk warning remains.
- **`render/pixels-4/pixels.json` and `render/supplement-3/supplement.json`: PASS**, actual native Windows Edge WebGPU r186. Prior adapter discovery identified NVIDIA/Blackwell/non-fallback. Zero captured errors and no deprecated PCFSoft fallback; R3F's Three.Clock deprecation remains.
- All nine modes have matched-time on/off particle contribution and distinct temporal contribution, aggregated in `final-render-summary.json`. Counted pixels above a2-channel-value difference: light rain1021, heavy rain2399, snow4080, ash1953, sparkles3245, wind1365, leaves1088, sand1517, embers3147. These are isolated-fixture contribution counts, not quality or FPS scores. Small rain/wind effects are subtle in downscaled contact sheets; no style retuning was inferred.
- Actual local/distant bolt atlas offsets advance. Synchronized normal-material captures show both bolt types, including geometry-only on/off comparison with point-light ownership retained. Initial blank atlas frame is not used as the visibility proof.
- Real SunLight/PCF map allocation and two cascades verified. Near12/far55 camera samples visibly show the cube and its shadow; far framing is independently image-inspected. No full-device/cascade-seam quality matrix claimed.
- Real `cat-agent-01.glb` Idle animation time and L_Thigh quaternion advance, materialization completes, and the imported biped cat is visible beside the fixture box. The actual `ImportedAvatarCanvas` reports ready and WebGPU186; its full textured model capture was image-inspected. Fixture layout/status styling is not the normal product layout.
- Bloom/fog mask readback: actual transparent-black NoBlending screen aperture **RGBA[0,0,0,0]**, outside control alpha255. A dark screenshot rectangle is the empty page behind that aperture, not evidence that it became opaque; no DOM content was mounted in this fixture.
- All eight Graphics flags exercised independently OFF/restore, with retained actor identity. Bloom targets drop16→5, reflection and base-fog owned targets drop on disable, related mesh removal is observed; all-off removes the postprocess owner. Fixture reproduces real city-owner switch/lifetime conditions without navigating the product. Settled arrival sparks do not replay on restore.
- Repository effects HDR readback contains **zero NaNs/infinities** after the color correction. Original floor/sky/box/reflection and corrected city captures independently image-inspected.
- No automated product-navigation journeys or unrequested live-provider dispatches were run. Full application visual/motion/listening/comfort acceptance remains Aaron's.

## Retained verification history

Do not flatten these earlier eras into final PASS:

1. Historical r185 weather baseline:253files/1496tests.
2. Intermediate renderer suite:31files/142tests. Core gates1/2 stopped at formatting/lint. Core gate3:1497/1499passes, two obsolete composer-AA tests failed; focused replacement5/5 and build1 subsequently passed.
3. Attempt9 was native error-free but preceded the last reflector edit. Attempt10 verified that edit's compilation/reflection and weather; later image inspection exposed a near-black city/imported capture despite clean logs. Its all-criteria visual claim is not authoritative.
4. `fog-diagnostic-3` isolated the remaining blackout to terminal rain, not the fog volume. `rain-red` asserts18840invalid half-float values. A speculative signed-pow rewrite did not fix it (`rain-green` remained RED) and was completely reverted. Real RGB binding fixes it: `rain-green-2` has0NaNs/0infinities and visibly restored scene.
5. `pixels-1` stopped on a Canvas2D diagnostic warning, not a product error. The observer uses `willReadFrequently` thereafter.
6. Earlier never-loop snapshots could show a prior frame because node PassNodes cache per renderer frame. Final captures yield across frames, render current state, await GPU submission and compositor presentation. Use pixels4/supplement3 for matched-time and near/far claims, not stale samples from prior attempts. All attempts remain retained.

## Cleanup and current TEST ownership

- Old weather45425/backend44025 and isolated45427/CDP49427 are closed. Re-attestation found no old test owners before deletion.
- **47 obsolete acceptance-only browser profiles removed**, including the old weather profile and native proof profile. `cleanup-profile-targets.json` and `cleanup-profile-result.json` enumerate exact targets and successful readback. WSL recursive preflight timed out before deletion; native Windows preflight/deletion completed. No broad acceptance-parent deletion.
- **All backend state, saved-project discovery/Workstreams/worktrees, source repositories, native history, installed voice providers, supplier packs and historical evidence remain.** Historical profile-reopen instructions are retired; these retained roots are not junk simply because their browser profiles are gone. The old weather root's two custom slots remain there, not imported into the fresh review.
- New service: `aiw-webgpu-review-20260924.service`, loopback frontend45429/backend44029, 3GiB/no swap/control-group. Serves normal current `apps/web/dist` and matching `apps/local-server/dist`; do not rebuild underneath it.
- New state/profile root: `/mnt/c/Users/Mela AI/AppData/Local/AgentIntersect-World/acceptance/aiw-webgpu-review-20260924`. Backend cwd is its disposable `repository`; native workspace directory exists. No old roster/setup/consent/browser state copied.
- Native Edge root14476/creation1790278710774, CDP49429, enabled/responding ownerless window3344840; wrapper `proc_ce1c61a572a4` / WSL1359517. Historical IDs are not future kill authority; re-attest with the current helpers.
- Read-only native observation: one exact app page, complete Agent Setup, empty local/session storage, viewport/scroll924×922, correct entry, no observed console/page errors. **Discover Agents** is enabled; Continue correctly disabled until setup. Screenshot image-inspected. Zero product clicks/typing/consent/messages. Initial observer wrongly awaited `identify` rather than the actual fresh Agent Setup; corrected observation did not reload or reset the page.
- Fresh setup API: completed=false, registrations=[], capabilities=[] until the user attaches connections. Optional voice reports not-installed/unavailable, matching the inherited launcher configuration. No installation or provider activation performed; user retains optional setup/consent.
- Hermes8642PID721 and OpenClaw18789PID722 retained. The review survives chat turns, not guaranteed host/WSL shutdown, sleep or reboot.

Kit: `~/.hermes/runs/aiw-webgpu-upgrade-20260924/review/`.

Read-only: `python3 <kit>/check.py`, `python3 <kit>/stop.py check`, `python3 <kit>/close-browser.py check`.

Only when closing/replacement is authorized: `python3 <kit>/close-browser.py stop`, then `python3 <kit>/stop.py stop`. Both re-attest exact owners and retain state. New stoppers' read-only paths were exercised; current review has intentionally not been stopped as a test.

## Remaining human/delivery gates

- [ ] Aaron's upgraded World appearance/motion/smoothness, lighting/reflections/fog/bloom/AA, weather/lightning and listening/comfort review.
- [ ] Carry forward scoped HUD clearance, both5-second notices, scene/custom-generation fidelity, settings/save/reload/cycling, Dance→swap→Bow→normal movement/chat, failure/cancellation/repeated generation and optional voice criteria. Reuse unchanged accepted evidence; do not require unrelated provider reruns.
- [ ] Commit/push/exact-head CI/PR-ready/merge only when separately authorized. No hosted verification for these local edits yet.
- [ ] Resolve new-image metadata and ElevenLabs plan-specific publication rights before public media delivery. Props remain10decorative cutouts, not meshes; true3Dprops/modeling and physical weather shelter/collision/splash simulation need separate scope. Source seams/softness/upscaling remain.
- [ ] Keep native Windows backend-package acceptance, named Hermes OAuth-profile/deep-native-path limitations, Workbench/history/layout/recovery/guide backlog, releases/installers/downloads and Phase20 separate.

Previous r185 transition smoothness/black-section acceptance is preserved at its actual scope. This migration has no blanket manual PASS, no native FPS improvement claim and no fallback-device matrix.
