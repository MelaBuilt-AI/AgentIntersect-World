# Local voice setup and saved-project library

## Current acceptance and delivery authorization

Aaron1550284421675098113 explicitly accepted the revised intact-artwork loader: “thats perfect! PASS!” and authorized commit/push, merge after exact-head GitHub CI and readiness are green, then end-session handoff. This supersedes historical merge-held and visual-pending wording below. The accepted slice includes the previously approved PTT clarity, local library, clone-folder form and continuous repository loading, plus the final floating intact image/separate orbit trails.

Readiness uses the fresh focused/type/lint/build/browser evidence and first-hand acceptance already recorded here, plus the complete required GitHub workflows on the final delivered SHA. Both push and PR workflow runs must be green before an expected-head guarded merge. Record final commit/run/merge identities externally in the PR and handoff to avoid recursive documentation commits. No tag/release/publication, provider/gateway changes, branch deletion or retained-test cleanup is authorized.

Full native Windows World/four-harness acceptance, separately observed dirty-work/native-conversation/preview Resume, explicit Git milestones, and state-preserving restart remain follow-up evidence—not claims established by this visual PASS. Preserve existing state and clones.

## Original authorized scope — September 17, 2026 (historical)

Aaron1550163615531794464 authorizes one new private PR and direct implementation. Base: main80a992c. At that initial authorization, merge, release, publication, gateway changes and modification of retained operator tests were held; the current conditional merge authorization above supersedes only the delivery hold. Development uses a separate normal Git worktree because the original checkout supplies live retained runtimes.

### Voice

- Correct the quick-click/no-recording case with exact text: `Nothing recorded. Left click and hold while talking.` Do not misclassify unrelated provider errors as silence or disclose internal file paths.
- Preserve left-button hold/release recording and keyboard accessibility.
- Right-click once starts hands-free recording. The caption surface exposes Send and Cancel. Send finishes capture/transcription and sends that explicitly accepted utterance once; it exits hands-free mode. Cancel discards capture/transcription and exits. The next hands-free session requires a new right-click. Preserve bounded audio duration/size, cancellation and no stale sends; no automatic re-arming. The local provider remains final-caption-only, not claimed streaming partials.
- Optional first-run Local voice setup plus an accessible later setup action: explicit install consent, immutable official runtime/model pins, safe private installation, exact-byte verification, durable activation, reusable readiness and honest unsupported/offline/failure states. No hidden package-manager postinstall, cloud transcription fallback, microphone permission or recording without user action.
- Support and test Linux x64 and native Windows x64 providers separately; browser on Windows + backend on WSL is not native Windows World acceptance. macOS/other architectures remain explicitly unsupported until verified artifacts exist. Native Windows integration failures must remain reported, not be papered over by WSL results.

### Saved projects

- Load Repo is a durable library of projects opened/worked on through World, not a filesystem-wide repository scanner or short recent-only cache.
- Show project identity/location, availability, latest saved work and meaningful work/Git milestones. Preserve unfinished and uncommitted work and conversation references, not only completed work.
- Explicit project selection/Resume reuses repository, saved Workstream, owned worktree, session/history and approved-preview recovery through existing authorities. Loading alone never dispatches coding. New Workstream remains separate.
- Prefer explicit project resumption over forced repository loading on World entry. Restore agent identity independently; optional failure must not destroy accepted sessions.
- Record completed-work and explicit Git checkpoints/commits as milestones. They describe saved truth; no automatic commits, destructive checkout, reset or historical-file rollback. Existing worktree/files remain authoritative.

### Verification and delivery

Focused RED→GREEN tests per behavior, affected type/lint/build gates, built-browser proofs and real provider installation/readiness/transcription. Run a native Windows backend/provider smoke independently of WSL and prepare manual Windows acceptance when available; no inferred full native-harness acceptance. Preserve all source residuals and TEST45365/45363/45353/45351 state and served builds. Open draft PR first; commit/push verified implementation and report exact CI state. Human microphone/visual acceptance remains separate.

## Implementation sequence

1. Voice short-input regression, hands-free capture/send/cancel UI and tests.
2. Pinned platform-aware provider provisioning, runtime default resolution, setup API/UI, tests and real installation smoke.
3. Durable project catalog/milestones and explicit Resume UI using existing Workstream continuation.
4. Integrated/browser/native Windows checks; docs/status; final PR update and exact-head CI.

## Operator feedback follow-up — September 17, 2026

Aaron1550238785432723660 tested TEST45367: hands-free speech worked after Send, but capture had no convincing audio feedback and no pre-send transcription action. He requested the following bounded UI changes (not overall acceptance or merge authorization):

- PTT shows small `L Click Hold` above `Push to Talk`, and small `R Click = On` below.
- While recording, show a live audio-level animation driven by captured microphone samples, with a truthful quiet/waiting state; retain `Final transcript appears when recording ends.` No streaming transcription or additional microphone access.
- Hands-free actions are ordered Send / Transcribe / Cancel. Transcribe stops recording now and produces an editable final caption without dispatch; Send accepts it once, Cancel discards it. Keep the existing direct Send and time ceiling; never rearm automatically.
- Saved project library shows Last opened above a labeled `Select Project` dropdown. Default to the greatest persisted lastOpenedAt, independent of pin ordering; render only the selected project's details/actions. Selecting another project changes the Open project target without loading/dispatching until clicked. Existing per-project saved-work selection, confirmation, dirty-state/milestone truth remain intact. Reload defaults to the last actually opened project, not an unconfirmed dropdown choice.
- Standard frontend slice: focused RED/GREEN, affected gates and isolated browser proof. Keep TEST45367 and older lanes, their state/profiles and loaded backend bytes untouched. No backend rebuild under the retained service, no replacement manual lane, commit/push/merge or release in this follow-up without separate authorization.

## GitHub clone feedback — September 17, 2026

Aaron1550254500416192643 confirmed NetBox cloned and loaded in TEST45369 with `Repository loaded locally · Current · 3 packages · 376 directories · 2210 files`. Earlier Windows drive/UNC destination attempts were superseded by the successful WSL path `/home/mela_ai/projects/Clone test`; this is Windows Edge + WSL backend acceptance, not native Windows backend proof. PTT changes and local loading/library UX were also accepted in preceding reports; reload/Resume criteria remain separately scoped.

Authorized follow-up: Discover path fills Clone destination with the discovered `projectsPath` (including when the folder does not exist yet; discovery remains read-only). Clone destination is an editable parent folder. Replace the clone-only optional friendly name with required `Folder Name to Create`; allow a single folder name, including spaces, not slash/dot traversal. Show the resulting child destination and send that full path to the existing clone API only on explicit Clone GitHub. Preserve existing local/create behavior and backend no-overwrite protections. No new path translation, progress redesign, backend rebuild, test-session restart, commit/push/merge, or changes to the retained NetBox clone.

## Animated repository loading indicator — September 17, 2026

Aaron1550261263961948252 accepted the clone-parent/folder-name form, then reported a wait in intake followed by about five seconds of blank World before the repository appeared. Authorized: reusable animated loading artwork, every repository-loading route, a fresh manual test left running, then end-session handoff. No commit/push/merge requested.

- Source artwork: `C:\Codex\3D avatars\AgentIntersect-World-Website-Assets-Expanded\4k\foreground\15-intersection-traces-4k.png` (RGBA3840×3840, original unmodified). SHA256 `edceac4db48ed70573021d94051b9eb46a34a0bdd505da8aea093b75c0396413`.
- Runtime derivative: `apps/web/public/assets/loading/intersection-traces.webp`, resized960×960 with Pillow LANCZOS, WebP quality92/method6,366244bytes, SHA256 `e13767abdf903acae0953baadf1097da86bb9b13381062927c5bf18524d338ac`. Same composition/transparency; no replacement/generated illustration.
- `WorldLoadingIndicator` is a presentational reusable component. The initial SVG-mask endpoint animation was rejected and replaced by the intact-image/separate-trail implementation below; the complete composition gently hovers. Consolas `Loading..._` types left-to-right once, then blinks the trailing underscore. Reduced Motion keeps the complete caption and still artwork.
- Repository overlay starts before clone/open/create/saved-resume work, persists across indexing and scene preparation, and ends on renderer readiness or visible failure—not a guessed timeout. Existing World and files remain preserved. No automatic retry/coding/clone or percentage claims.
- First browser RED confirmed no loading indicator while the clone request remained pending. Focused component/UI tests, affected typecheck/lint/format and production build passed. Three built-browser gated journeys (clone/open/create) passed in `/tmp/aiw-pr15-loader-green2`; saved-project/workbench regression passed separately in `/tmp/aiw-pr15-loader-library`. The three lifecycle tests hold intake, indexing and model requests independently, preserve one indicator mount through transitions, then require renderer-ready/indicator removal. Different rendered animation frames, caption/cursor, Reduced Motion and desktop/portrait containment passed; screenshots inspected.
- Initial browser run failed only on an incorrect final `Repository floor` assertion; actual terminal label is `Repository city · ready`. The workbench journey then needed to await actual render readiness before its next pointer action instead of the early floor label. Both were test corrections; no production bypass or skipped assertions.
- Authorized fresh TEST http://127.0.0.1:45373/ was launched for review; functional loading subsequently passed and the initial endpoint-animation style was rejected; kit `/home/mela_ai/.hermes/runs/aiw-pr15-loading-review-20260917`. Native Edge + WSL backend, fresh profile/app state, copied verified local voice, unchanged backend. Direct/proxy health, actual served entry, empty setup/storage and untouched entry pixels verified. TEST45371 and existing clones preserved; no commit/push/merge.

## Intact-artwork loader correction — September 17, 2026

Aaron1550274257836183604: loading “works great,” but the independently animated endpoint orbs look strange and are rejected. Preserve the functional loading PASS; replace only the presentation. The original supplied composition must remain intact and gently float, with separate rapidly rotating swirl effects around it. No detached or masked artwork endpoints.

- Routine presentation-only scope: reuse the complete optimized derivative of the exact supplied original PNG, one unmasked image, twelve separate thin cyan/violet/gold orbital trails with staggered sub-second/near-second rotations, existing whole-image hover and typed/blinking Consolas caption. Preserve loading lifecycle, state, provider and backend behavior. Reduced Motion disables motion.
- Focused regression reproduced the prior five image layers versus one intact image; updated component tests pass. Frontend non-emitting typecheck, affected ESLint and production build pass. Three staged clone/open/create browser journeys pass in `/tmp/aiw-pr15-orbit-proof2`, including intact-image/no-mask assertions, separate trail motion, distinct rendered frames, reduced motion and desktop/portrait captures inspected. An initial temporary test config selected zero tests; the next failed server startup due to its `/tmp` cwd. Corrected only the proof config and reran successfully; neither was a product failure.
- Fresh native Edge retest is LIVE at `http://127.0.0.1:45375/`, kit `/home/mela_ai/.hermes/runs/aiw-pr15-orbit-review-20260917`. Fresh state/profile, copied verified voice install, unchanged WSL backend. Existing TEST45373 and older lanes/clones preserved. Revised visual verdict PASS from Aaron1550284421675098113; commit/push and conditional green-CI/readiness merge are now authorized. Direct/proxy health, exact served bytes, zero setup registrations/projects and copied voice readiness verified. One exact-profile visible enabled native Edge window verified; CDP49375 did not bind (confirmed from Windows and WSL), so native DOM/storage/screenshot observation is unavailable. Browser was not reset or relaunched.

## CI reconciliation after acceptance

The first accepted-feedback push exposed legacy browser journeys interacting before the new inert loading barrier released input. Owning tests now await scene/repository renderer readiness before keyboard/fill/focus or wheel input; the delayed-city spatial fixture releases its held assets before the newly gated interaction while retaining the delayed-load proof. The cadence source-text guard is scoped to its measurement function rather than matching across an unrelated loading-animation callback.

Two concrete defects were also reproduced and corrected: the taller mobile voice/chat HUD needed additional Workstream-panel clearance, and Phase 14 preview cancellation during persistence could otherwise spawn after cancellation removed its disposable directory. The latter has a deterministic held-persistence regression proving cancelled truth and no child spawn. No loader artwork, animation, workflow gates or assertions were bypassed.

Local correction evidence: 216 files / 1,324 unit tests passed; affected lint, formatting and non-emitting root/web/server typechecks passed; rebuilt flagged frontend passed two focused browser journeys, then all twelve remaining affected journeys including the headed pointer-lock cases. Mobile panel/HUD screenshot was inspected. GitHub replacement workflows and final merge identities remain external receipt gates, not inferred from these local results.

## Status

The base implementation and accepted operator-feedback follow-up are complete and locally verified in PR #15. Final delivery is authorized with an exact-head CI/readiness gate before merge. Baseline233d746 CI is historical, not proof for later feedback changes; use the external PR/handoff receipt for the final delivered SHA and terminal CI/merge result. Retained tests remain on their served builds. Scoped human passes are recorded above; broader native Windows harness/continuity acceptance remains separate.

## Delivered behavior

- Left hold/release and keyboard PTT remain supported. A too-short recording shows the exact no-recording message above. Right-click starts one hands-free capture; Send finishes and dispatches once, Cancel discards, neither rearms.
- **Local voice setup (optional)** appears in first-run Agent Setup and remains reachable through the later Agent Setup menu. Opening setup only reads readiness. Installation requires its own checked consent. It does not request microphone permission, enable voice, or change the agent/harness setup.
- The managed provider defaults to a private versioned directory under `$XDG_DATA_HOME/AgentIntersect-World/voice` (fallback `~/.local/share`) or `%LOCALAPPDATA%\AgentIntersect-World\voice`. `AIW_LOCAL_VOICE_DIR` overrides this directory for isolated tests. Existing `AIW_PHASE15_STT_PROVIDER_ROOT` remains an explicit legacy-provider override; remove that override to use the managed install.
- Official whisper.cpp **v1.9.1** Linux/Windows x64 CPU artifacts and English `base.en` model revision **5359861c739e955e79d9a303bcbc70fb988958b1** are pinned by size and SHA-256. Exact runtime member/license inventories are in `packages/voice/src/runtime-pins.ts`; model pins are in the declared Node-only voice entrypoint. Only the CLI and its allowlisted runtime/license files are materialized. Approved Linux shared-library aliases become regular files. No bundled server, PATH change, package postinstall, cloud fallback, or automatic version update.
- Downloads stage privately, verify before activation, and are cleaned on failure. New provider instances discover the verified versioned installation. Existing damaged installations are not overwritten in place; setup reports that explicit repair is required. macOS/non-x64 show unsupported and leave typed chat available.
- The saved project catalog no longer evicts the oldest unpinned entries after 50 opens. It reports missing/unavailable directories without removing them, shows saved Workstreams including dirty work and native conversation references, and combines retained Workstream milestones with durable explicit Git checkpoint/commit milestones.
- **Open project** loads only the selected repository. **Resume saved work** requires confirmation, verifies the saved agent/native root, reuses existing continuation/worktree/preview authority, restores the conversation, and never sends a coding turn. An unavailable saved session is not silently replaced. In multi-agent World, reconnect the saved agent to the constellation first. New Workstream is separate.
- Normal refresh restores accepted agent identity/history into a blank World; repository selection is explicit through Load Repo. No automatic commit, reset, checkout, or historical rollback was added.

## Base implementation verification (historical, before feedback)

- Full unit/integration suite: **215 files, 1,319 tests passed**.
- Repository-wide formatting, lint, typecheck, architecture, build, and smoke passed. The architecture gate caught the installer in browser-reachable source; it now lives in the existing declared Node-only entrypoint rather than weakening the gate.
- Built-browser checks: optional setup consent/no microphone, existing grouped PTT, hands-free Send/Cancel/no rearm, project intake, saved-work/preview/library Resume with zero coding requests, and identity-only refresh passed. Library screenshots were inspected at desktop and portrait sizes; later scoped human acceptance is recorded above.
- Fresh Linux x64 and **native Windows x64 Node 24.18.0** Fastify setup/provider smokes both went from not-installed to ready, rejected missing consent, remained ready from a new installer instance, transcribed the public sample, and left zero volatile audio files. Sanitized measurements: `artifacts/pr15/local-voice-smoke.json`.
- Windows proof used an isolated portable official Node executable and bundled the tracked smoke with its actual route/provider dependencies. It did **not** run a Windows browser against WSL as a substitute for native backend proof. It is not full Windows World/harness acceptance and makes no Windows peak-RSS claim.
- No changes to the original live checkout, retained TEST45365/45363/45353/45351 sessions/builds, gateway configuration, repo visibility, or release state.

## Follow-up verification

- 58 focused tests across eight files passed, including sample-level silence/audio/reset, PTT markup, 80-project single-card/default-last-opened, and actual lastOpenedAt persistence through backend recreation. Frontend typecheck, affected lint/format, production frontend build and diff checks passed. No backend build or provider change.
- Built-browser hold/touch/keyboard final-caption routing and hands-free direct Send/Cancel/no rearm passed. Extended hands-free proof drove silence → audio → silence through the real capture controller, asserted bar-height changes, then Transcribe → editable caption with zero dispatch → edited Send once. Recording/final screenshots inspected.
- The expanded repository/workbench journey passed against the same production artifact: 80 options, one card, selection alone sends no open request, explicit Open targets the selected path, reload defaults to its updated authoritative lastOpenedAt, and existing dirty-work/confirmed Resume/native-history/preview/no-coding behavior remains intact. Desktop and scrolled portrait pixels inspected.
- Browser attempts preserved under `/tmp/aiw-pr15-feedback-final-browser` and `/tmp/aiw-pr15-feedback-library-final`. Two test-harness corrections were needed: use the combobox accessible name rather than exact wrapping-label text (which includes option text for getByLabel), and complete this fixture's non-persisted avatar-consent gate after reload. No production workaround, skipped assertion or deadline increase. The two voice journeys passed in the combined run; the library journey passed separately after those harness corrections.
- TEST45367 read-only ownership, direct/proxy health and original served-index hash passed; its state/profile/page were not reset or operated. Automated test listeners43770/45173 are gone. No commit/push/merge or updated manual lane.

## Clone-folder follow-up verification

- RED: the rendered-label test failed on missing `Folder Name to Create`; the production-browser test failed because Discover path left Clone destination empty.
- GREEN: nine focused tests across dialog/client/backend-intake files, frontend non-emitting typecheck, affected ESLint/Prettier, production frontend build, and diff checks passed.
- Four built-browser journeys passed together: local intake, clone-parent/required-child-folder with POSIX and Windows path fixtures, and saved-library/workbench continuity. Clone proof covers read-only discovery (including missing projects), invalid/empty names with direct-submit refusal, editable parents/trailing separators, spaces, exact outgoing child path/name, and explicit clone → repository-floor transition. Clone HTTP responses are fixtures; the operator's prior NetBox clone is the real-network acceptance, not a new network clone from these tests.
- Desktop/portrait captures are retained under `/tmp/aiw-pr15-clone-folder-green`; portrait inspection confirms the full clone form, action and wrapped destination preview remain readable in the scrollable dialog. The desktop capture is partially scrolled and does not show the entire form. Automated listeners43770/45173 closed after the run.
- TEST45369 ownership/proxy/served-index remained unchanged. No backend rebuild, user-session interaction, NetBox file changes, commit, push or merge. This was local-only verification at that time; Aaron subsequently accepted the clone-folder form before the loader follow-up.

## Broader acceptance checklist (separate follow-up evidence)

Use a fresh disposable app-state directory and unused ports; do not repoint retained operator lanes. Build this PR, start the backend with native Node on the target OS, and confirm its setup response reports that OS before testing.

1. Open optional Local voice setup. Decline/leave consent unchecked: no download and no microphone prompt. Consent and install, then restart the backend and confirm ready.
2. Enable voice separately. Quick-click PTT: exact no-recording message. Hold and speak: final caption. Right-click and speak: Send transcribes/sends once and stops; Cancel sends nothing and stops. No streaming-caption claim.
3. Load a disposable project, start work, and leave uncommitted changes. Refresh: identity/history return but the floor starts blank. Load Repo shows the saved project, path, availability, dirty work and conversation.
4. Confirm Resume: same Workstream/worktree/native conversation, unfinished file bytes preserved, approved preview recovery attempted, no coding turn. Explicit checkpoint/commit adds a milestone. A missing project or disconnected saved session fails visibly without deletion/replacement.
5. Run that complete browser/harness path separately on native Windows before claiming full Windows product acceptance. Backend/provider smoke alone does not close this gate.

The bounded provider smoke is `apps/local-server/scripts/exercise-local-voice.ts`, taking a private install directory and a plain 16-kHz mono PCM16 English test WAV. It uses an ephemeral loopback port and always closes its server.
