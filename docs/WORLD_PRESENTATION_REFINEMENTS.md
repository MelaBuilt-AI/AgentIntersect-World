# World entry and conversation presentation refinements

> **September 9 delivery update:** Aaron authorized the cumulative PR, push and conditional merge after CI/readiness are green (`1547254171474993302`). The current delivery/acceptance cutline is `PROJECT_STATUS.md`; dated no-delivery and pending-test statements below are historical records, not current authorization. Retained runtimes, public/release actions and later-phase scopes remain protected.

Source: Aaron Discord1546882401836212315, 2026-09-08. **User PASS:** same-Workstream second change completed; focused sky glitch gone. Reported validation is user-relayed, not a newly executed native test. Other unmentioned criteria are not newly accepted.

## Authorized criteria

- Historical first Enter World flash of blank grid/sky and T-posed models: readiness-driven transition, not a fixed arbitrary delay. Reveal once scene textures and all selected avatar initial poses are rendered. Preserve semantic fallback/reduced motion; later asset loads must not blank an established World.
- Open wheel below zoomed/focused code inspector, preserving actual HUD/input and unfocused wheel behavior.
- Concise readable work reports without raw inline validator scripts or mid-sentence cutoff; details remain available in Inspector. No fabricated validation success. Completion refresh follows terminal evidence, with truthful waiting indication rather than pretending the agent is still digging after tools finish.
- Clearer bubble text at typical camera distances without changing avatar art or spatial-screen behavior.
- Explain genuine harness output vs World-generated status and identity/context. No provider/profile/persona mutation or fake Mr Fluff identity for Codex.

Current45285 and older user lanes/site remain intact. No reset, replacement browser, native dispatch, publication or end-session rotation requested in this turn. Local edits and focused production-shaped automated checks only; source changed does not update the preserved frozen user lane.

## Implementation and evidence — 2026-09-08

- **Entry:** a dark transition covers the initial scene. WorldEnvironment waits for all four current texture objects plus readiness from every selected user/agent avatar, then three rendered frames; WorldRoom fades the cover (Reduced Motion has no fade). Both renderer paths participate. Existing World is not re-covered on later loads. A stalled load offers text-only continuation and labels it as a user skip, not a false asset failure.
- **Focus:** focused source viewport z15 is above wheel z12 while real HUD remains above. Regression was RED at z10; GREEN includes an actually open overlapping wheel and native pointer-lock journey.
- **Reports:** the command wall was World-generated `workstreamOutcomeSummary`, not proof of native-agent personality. Use short changed-file and validation bullets with recognizable inline-script/test/diff labels; retain complete command/output evidence in Workstream Inspector. Remove the redundant raw final-activity append and its mid-sentence512-character cut. An integration test checks a long inline command remains in validation evidence but not in the report, and ends with the complete Inspector pointer. Report projection now occurs whenever current Workstream state is published, not solely from a separate polling callback. Native final-answer generation/authority polling can still take time; no native timing SLA or artificial Dig hold was introduced.
- **Bubbles:** the first larger high-resolution sprite still looked blurry in a real distance capture because the WebGL framebuffer uses reduced DPR. That attempt is retained as a **visual failure despite green size tests**. Final shared `useActivityBillboard` projects safe textContent DOM lettering at16px into the canvas host, clamps it inside the viewport, does not intercept input, and cleans up on unmount. It stays below focused screens, wheel and actual HUD. Both renderer paths have DOM/distance/cleanup regressions; final far-distance browser capture uses actual backward/lateral walking and keeps normal HUD visible.
- **Identity:** connected native harness/session output is distinct from World-generated operational reports. Codex is a genuine harness; name/avatar alone does not configure personality or reproduce another session's memory. No Hermes/Codex profile, persona, session or prompt configuration changed here.

### Final verification

Evidence root: `/home/mela_ai/.hermes/runs/aiw-presentation-refinements-20260908/`.

- Final `gates.json`: runtime/packages/web-types/server-types/tests/build all0; **1184 passed,0 failed,0 skipped** after the DOM bubble correction. Affected ESLint and diff checks passed.
- Final **7 browser journeys passed**: six in `browser-attempt-2`, plus the normal-Workstream/distant-bubble journey in `distance-attempt-3`. Coverage includes held texture/reveal, stalled-load fallback, preview/menu/report formatting, focused code with open wheel, and both pointer input guards.
- `final-verification.json` aggregates exact tests and final results. Earlier sprite captures/receipts, first compile failure from a temporary Three type gap, and pre-DOM gates remain explicitly historical—not final visual evidence.
- Directly inspected entry, report, focused screen and final `distance-attempt-3/.../bubble-far.png`. Final bubble says “Working / Editing settings.tsx” sharply despite the distant, low-DPR scene; this is an explicit fixture, not native Codex execution.
- Preserved historical `artifacts/phase18/repository-floor-pointer-lock.png` bytes restored after fixture capture. Browser proof servers exited. Existing45285/45283/45281/45279/site46381 remained200; direct43885 `/health` returned200. A mistaken direct `/api/health` probe was404 and is not a product failure.

### Delivery boundary

The corrected build is `aiw-presentation-refinements-20260908/web/`. After Aaron1546900180811382854 authorized a new test, it was activated in **http://127.0.0.1:45287/** / backend43887 with fresh isolated state, Codex capability and a dedicated Edge profile at untouched Create Avatar. See `/home/mela_ai/.hermes/runs/aiw-presentation-manual-20260908/README.md`, runtime/readiness/native-window receipts and entry screenshot. Copied three-file website passed its existing test and is checkpointed; no previous Workstream/native history imported. Zero user-lane clicks, agent dispatches or preview approvals by the assistant. Old45285 and older lanes remain intact. User requested leaving the new test running and end-session prep; visual/native acceptance is still pending. Nothing staged, committed, pushed or published.
