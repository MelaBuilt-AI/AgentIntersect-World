> **Historical record only.** Superseded by [HACK_YOUR_WORLD.md](HACK_YOUR_WORLD.md). Earlier RUNNING, pending, scope and media-origin statements describe their recorded attempts, not current authority. Original narrative follows intact; no old failures are promoted to PASS.

# Hack your World — custom authoring

## Current normal test — TEST45447 RUNNING

**Latest first-hand verdict — distant lightning visually ACCEPTED.** Aaron: “nailed it Fluff! love the distant lightning now!” (Discord `1553070041526829118`). This accepts the new distant horizon-lightning appearance, preserving prior FPS/rock-shadow/existing-lightning acceptance. It does not independently establish other settings, follow/Run, persistence, or delivery gates. TEST45447 stays running; no cleanup, replacement, Git delivery, or release was requested. Earlier horizon-pending wording below is superseded by this scoped verdict.

Aaron authorized replacement in Discord1553062537548005447. The verified horizon-lightning frontend and matching compiled backend are now running at **http://127.0.0.1:45447/** (backend44047, Edge CDP49447, service`aiw-horizon-review-20260925.service`). Fresh setup/profile/state is at`C:\Users\Mela AI\AppData\Local\AgentIntersect-World\acceptance\aiw-horizon-review-20260925`; launch/ownership/cleanup kit`~/.hermes/runs/aiw-horizon-lightning-20260925/review/`. Frontend entry index-D6dVs-1Y.js matches tested bytes; backend uses candidate`server-dist/index.js`, not older source dist. Scoped original FPS/shadows/upper lightning acceptance remains; new horizon appearance/pace pending.

Old TEST45443 services are stopped and their three owned processes/listeners exited; its native browser was already closed. All old profile/state/work roots remain retained. Fresh setup reports zero registrations/roster; one enabled native Edge window, empty storage, Discover Agents enabled, Continue disabled, zero observed page errors. No app clicks, agent attachment, provider activation or user-state import occurred. Optional local voice remains unavailable/not installed, as before. Read-only `check.py` and `stop.py check` plus profile-bound `close-browser.py check` pass. Explicit cleanup only: `close-browser.py stop`, then `stop.py stop`; retain state. Below are historical pre-launch statements where superseded by this current runtime notice.

## Latest addition — low horizon storm band verified separately

Aaron accepts current FPS/rock shadows/existing lightning. Added optional `weather.horizonLightning` with independent density, interval and elevation: smaller quick flashes above visible mountain silhouettes, preserving upper/local lightning. Controls, prompt guidance, truthful report and saved-slot/restart persistence are verified;236affected tests/11architecture and native visible/Reduced Motion proof pass. **Current TEST45443 stays untouched on its prior accepted build; new candidate is not installed yet.** [Authoritative report and candidate paths](THREE_WEBGPU_UPGRADE.md).

## Current September25 review — TEST45443 RUNNING

Fresh http://127.0.0.1:45443/ is open at Agent Setup for Aaron; leave running. Old45439closed with state/work/profile retained. Added cutout silhouette/contact shadows, sky-wide3–8bolts/arcs/glows with corrected stationary atlas updates, real follow Run catch-up speed, and removed redundant fog shadow passes without effects/quality reduction.232affected tests/11architecture and native visible/fidelity proof pass; new hands-on verdict pending. Prior transition/improved-running/local-lightning acceptance retained. [Current source, evidence, caveats and runtime authority](THREE_WEBGPU_UPGRADE.md). All runtime pointers below are historical.

## Current direction — r186 rendering/stall corrections; fresh retest RUNNING

Aaron's TEST45429 manual FAIL for white Original textures, dark avatars and first-use materialization/shadow/stream stalls was corrected. **Fresh TEST http://127.0.0.1:45433/ is RUNNING for Aaron**, untouched Agent Setup; old45429closed/state retained. Stable node-material ownership, indirect fill and actual nested-pass/GPU-complete preparation are verified. Full1507tests/11architecture PASS before final GPU-completion guard; affected final153renderer tests plus format/lint/types/build/startup/API PASS. Native fixture max13.4msavatar/13mscity-stream; no whole-product FPS claim. Corrected manual verdict PENDING. [Current report and owner commands](THREE_WEBGPU_UPGRADE.md). Optional voice user-controlled/not-installed; work/native history/providers retained. No Git delivery/publication/installers/Phase20 authority.

## Historical September 24 expansion/weather baseline

The former weather TEST45425 service/backend and exact Edge window are closed. Its old browser profile was deleted; backend state and two custom slots remain retained, not loaded in fresh TEST45429. All running/retention directions in historical sections below are superseded by the current migration report. The baseline evidence remains valid only at its recorded r185 scope.

Final conventional gate PASS: **253 files /1496 tests**,11architecture,format/lint/types, normal build and startup/API smoke. Entry404614bytes stays below409600; weather audio is lazy-loaded and local audio paths resolve on demand. Earlier startup-budget RED411020, first split410234 retained; final unchanged budget passes. [Full implementation/intake/verification](HACK_YOUR_WORLD_WEATHER.md). All58runtime media served byte-matching;26OGGs decoded. New manual verdict PENDING; prior transition smoothness/black sections accepted.

Aaron (Discord1552713775969472565) first-hand accepts improved transition smoothness and complementary black sections. Two reported failures remain: Project / Current Work HUD overlaps the Hack controls/notices; use-without-saving notice persists. Correct both (notice expires after5000ms; HUD clears the full control/message region responsively).

Authorized now: non-destructive full intake of both supplied v2 packs, generator/catalog integration, light/heavy rain, ash, sparkles, wind, snow, blowing leaves and matching sand/embers; optional distant sky lightning/flashes and local cosmetic strikes with supplied animated impacts/dust/sparks/scorch and matching event audio. Keep particle counts, flash rate and owned resource lifetimes bounded; Reduced Motion suppresses animated weather/lightning. Existing effects mute/unlock remains authoritative. Weather choices must survive validated recipes and custom slots, not only descriptive prompt text.

Inventory:436visual-pack files and117audio-pack files.14ready scenery images,10concept-only prop PNGs (no GLB meshes),8FX atlases covering10sprite layers,15ambience loops and11event sounds. Optional prop imagery may be exposed explicitly as decorative cutouts, never fabricated meshes. Preserve source files and provenance; texture seamlessness and audio listening acceptance remain caveats. Private local integration only; commercial/publication rights need resolution before asset distribution.

Standard local feature scope: Three0.185.1 unchanged; no r186 upgrade, SunLight/WebGPU, providers/native-session probes, collision/gameplay damage, repository authority, publication/commit/push/merge/installers/Phase20. Build separately from the live web-dist, preserve retained custom slots/state, then owner-verified replace/open the requested new review and prep end session. Conventional code and isolated component/renderer evidence precede user-owned product acceptance. Earlier sections below are historical where superseded.

## Historical smoothness-only review — superseded by weather review above

Aaron requested the corrected test and end-session handoff while he reviews it. **http://127.0.0.1:45425/** now serves the final r185 smoothness/black-patch/timed-notice build from `~/.hermes/runs/aiw-transition-smooth-20260924/web-dist`, not source `apps/web/dist`. Current owner kit `~/.hermes/runs/aiw-transition-smooth-20260924/review/`, service `aiw-smooth-review-20260924.service`. Same browser/profile/state and one saved custom slot preserved; actual served chunks and proxy health verified. The prior polish service is stopped and its receipt historical. LEAVE this new lane running and do not rebuild its served directory, reload the browser or automate review. Manual verdict for the newest changes is PENDING. r186 remains evaluated only.

## September 24 follow-up — transition smoothness, timed confirmation and r186 evaluation

Aaron accepted the transition appearance (“just how I would like it”), then reported variable stutter in `hack stutter.mp4` and requested a five-second saved-slot notice. His later additive direction asks for complementary black floor/sky sections between streaming-code flashes. The new black addition remains subject to his appearance verdict; do not erase acceptance of the previous effect.

Implemented on pinned Three0.185.1: save-success notice expires after5000ms; Original's texture-bearing environment remains mounted but hidden/inactive outside Original, and one shared bloom/AA render owner survives cosmetic swaps. Candidate texture GPU initialization is paced one per yielded frame while the old World remains visible; cancellation rejects late preparation. Code and complementary black cells use disjoint thresholds on environment materials only. No avatar/UI cover, resolution/effect reduction, weather implementation or dependency upgrade.

Final code gate PASS: format/lint/types,251files/1493tests;11architecture passed earlier in this same structural pass. Rebuilt normal web output separately under `~/.hermes/runs/aiw-transition-smooth-20260924/web-dist` because TEST45425 remains live on the earlier dist. Isolated component clock verifies present at4999ms/gone at5000ms. Post-black-patch renderer lifecycle comparison still retains Original object/texture identity and reduces framebuffer creations48→2 over Original→Sunlit→Mars→Original. Captured shader/page errors0. These are structural workload/isolated pixel proofs, NOT a native frame-rate or every-hitch-eliminated claim. Raw `uploads` counter in that private probe means `texImage2D` calls, not every kind of texture transfer. An early stale-frame harness receipt was rejected and preserved; final proof waits on authoritative phase AND target identity.

Read-only live review remains healthy and unchanged. No restart, browser takeover, saved-slot mutation, source-dist replacement or delivery performed. Next review requires coordinated replacement with the staged r185 candidate, preserving the existing profile/state/custom slots.

The separately requested **non-activating r186 evaluation** is complete in [THREE_R186_COMPATIBILITY.md](THREE_R186_COMPATIBILITY.md): verified official artifact,294runtime source-file scan, isolated aliased production build,29renderer files/140tests PASS, actual r186 isolated rendering with the indirect R3F PCFSoftShadowMap fallback warning reproduced. Dependency manifests/lockfile/installed revision remain unchanged. Recommended next step is a separately approved controlled upgrade with explicit PCF shadows and affected native visual checks—not an automatic WebGPU/splat/weather migration.

## Earlier September 24 — additive transition/control polish and PC custom slots

Aaron tested the prior review and reports it is “so much better”; preserve that scoped positive verdict. His requested additions are implemented locally: existing interference plus irregular floor/sky terminal-rain flashes, orbiting globe sparks accelerated during transitions, click ripple, right-hold progress indicator and eight install-local numbered custom slots. [Polish scope](HACK_YOUR_WORLD_POLISH.md).

After a successful preview, choose a slot and **Save to Custom**, **Use without saving**, or **Revert**. Occupied-slot replacement requires its checkbox; removal requires confirmation. No oldest-item eviction or name-based overwrite. Slots are data-only JSON files under the configured World data root's `environment-library/`, not browser localStorage; readback precedes the saved/cycling state. Empty slots are skipped. Earlier browser-saved recipes remain explicitly importable through Custom slots on this PC; their originals are not deleted.

Verification: format/lint/types, 11 architecture tests, **249 files / 1490 tests**, normal production build and startup/API smoke PASS. Entry bundle stays403.87kB (unchanged budget). Real disposable API persistence/restart, isolated React control save/readback/reload/cycling/replacement/hold/reduced-motion checks PASS. Isolated Sunlit/Mars/Original shader renders have zero captured errors; box actor remains visible and localized code patches are visible. These are not full-avatar/native performance, listening or comfort verdicts. No automated product-navigation journeys or native-agent probes ran. Usual optional-chunk size warning retained. Evidence: `~/.hermes/runs/aiw-polish-20260924/`.

Runtime correction: before this pass's build, TEST45425 unit was already inactive/MainPID0 and45425/44025/45419 had no listeners. No review service was stopped or saved state reset by this pass. No review service was restarted. Source dist now contains the new build; old served-byte receipts are historical. Any next review launch must preserve retained state and acknowledge the new build.

Asset/audio recommendation only: prioritize sharper horizon silhouettes and biome-specific prop variety; ocean, flowing water, forest/night insects and icy creaks add useful audio coverage. No new assets/providers/installers introduced. No commit/push/merge/release/Phase20 authority.

## Historical September 23 checkpoint — correction build

Aaron authorized completion of the visual corrections, restricted custom generation, WSL and Windows-native Codex/Claude selections, fresh normal testing and an end-session handoff. The implementation and conventional code/build gates are green. **Windows-native Claude's complete selected-agent path passed after Aaron's native Windows reauthentication; all six requested native paths now pass.** Hands-on visual, temporal, audio and comfort acceptance remains pending. Work stays local/uncommitted on `feat/hack-your-world`, base `f3755fd`; draft PR26's old CI does not cover this source.

Aaron's earlier Codex connection and icy/barren/windy plus another custom creation passed first-hand. Preserve these scoped successes rather than resetting all acceptance. His reported corrections now have material-local interference, sharper distant layers, seam corrections, terrain-sampled detailed props and clearer editor wording. Final rock-normal regression and isolated rendered inspection passed.

## Operator experience

- Original World remains the initial default. Left-click the globe under Escape for Menu to cycle Original, Sunlit Trails, Martian Expanse and explicitly saved recipes.
- Hold right-click, or use Shift+F10/ContextMenu, to describe a World in 1–500 words (12,000-character cap). Multi-agent users choose the designer. Only explicit Create dispatches the description.
- The selected actor's existing Dance loops during generation and loading, with “Hacking your World!”; material-local floor/sky interference keeps actor bodies visible. Faint screen static is an accent, not an opaque cover. Other actors retain their state; the selected actor's movement progression pauses rather than inventing/cancelling movement commands.
- The actual loaded swap starts Bow, “Enter World.”, the final sound and an application-projected completion report together. Animation ownership expires and ordinary movement/Idle/work resumes. Reduced Motion uses static status/Idle without animated interference.
- Save to Custom writes to one of eight explicit PC slots; Use without saving keeps a temporary preview, and Revert returns to its predecessor. Cancellation, invalid output and failed resource loading retain the previous World and reject late results. Exit releases owned textures/audio.
- The optional advanced recipe editor explains that JSON contains visual settings, not executable code. Starting-point fills the current recipe (Sunlit fallback for Original) without contacting an agent or applying a World. Copy brief, Preview, Keep and Revert remain distinct.
- Effects mute/autoplay unlock governs creation audio; music stays separate. Existing scene-matched storm, rain, wind, birds, alien, Mars and coding ambience is reused. Further atmosphere sounds await supplied assets; no new audio/TTS provider is enabled.

## Library and rendering

The 48 supplied library assets comprise 14 ground materials, 6 linear masks, 6 backgrounds, 6 horizons, 8 celestial sprites and 8 atmosphere fields. Supplier files/scripts remain untouched. Runtime WebPs and provenance live in `apps/web/public/assets/environments/library-v1/manifest.json`; the import utility validates image dimensions, alpha, wrapping and hashes. Larger masters are upscaled, not native 4K detail. Only the selected bounded layer set loads.

The data-only `aiw.environment/1` schema supports independent ground/blend/mask settings, world-meter UV scales and up to three celestial sprites. Color art is sRGB and masks linear. No arbitrary paths, URLs, scripts, GLSL, PBR/displacement claims, repository mutations or collision changes.

Current shared rendering corrections apply equally to preset and generated recipes:

- Original/scenic environment materials receive scoped interference; actor and UI subtrees do not. Injected GLSL locals are block-scoped to avoid existing shader-name collisions.
- Space/starfield backgrounds use procedural sharp stars. Full foreground spheres, feathered edges and corrected vertical wrapping remove the old straight upper cutoff; horizon coverage/placement improves distant mountains.
- Curved multi-blade grass and irregular subdivided rocks sample the actual ground/base/blend/mask, coordinated tint and surface detail. Coincident rock vertices share averaged normals rather than independent triangle-face normals.
- Fixed scatter budgets remain 24 grass / 18 rocks. Source mountain softness, sparse stylized props and finite flat terrain remain limitations. Isolated box-actor captures are shader/occlusion evidence, not native avatar, movement, FPS or comfort acceptance.

## Restricted generation and native selection

The session-scoped GET/POST `/agent-sessions/:sessionId/environment` uses the selected stored connection and the normal browser `/api` proxy. It shares the exact-session busy guard, validates the returned recipe and releases ownership in `finally`. It never resumes or rewrites the coding conversation.

The stateless `/environment/mcp` JSON-RPC endpoint exposes `describe_environment` and `propose_environment`, validates data-only proposals and applies no scene directly. It is not installed globally. Discovery, tool restriction, creative fidelity and user acceptance are separate evidence layers; no broad-chat fallback exists.

- **Codex WSL and Windows native:** selected launcher/profile, ephemeral restricted turn, read-only private working directory, no user rules/config/MCP/plugins/hooks, disabled command/web/image/delegation tools, and a temporary selected-model catalog removing shell/apply-patch surfaces. Uses `gpt-5.6-sol` with high reasoning, preserving this feature's existing policy. Native `CODEX_HOME` retains canonical authentication ownership; no rotating refresh credentials are cloned and discarded.
- **Claude cloud WSL and Windows-native transport:** safe-mode, empty tools/strict empty MCP, disabled hooks/skills, no session persistence, private cwd and the selected profile's provider/model. Native `CLAUDE_CONFIG_DIR` owns authentication refresh. Normal WSL bootstrap now accepts bounded exact-session `commands_changed` telemetry before init without weakening init/result/identity checks. The legacy local-Ollama model selection fix remains separate from cloud evidence.
- **OpenClaw:** the selected installation's public `simple-completion-runtime` SDK prepares the selected agent/model/auth and runs a host-prepared zero-tool completion, not `agent exec` or ordinary chat. Native auth ownership is retained without copying SQLite/refresh material. A plain Node child avoids TS-loader/plugin resolution interference; output is bounded and the final result framed separately from native logs. No gateway restart or configuration change.
- **Hermes:** private CLI config/state with explicit empty tools, no MCP/plugins/rules/memory/history, selected model/provider and native default-root auth read-through. The temporary home lives under the native root, allowing native refresh ownership rather than copying tokens. Differently rooted named OAuth profiles remain explicitly unsupported; do not silently use another account.
- **Cross-environment Codex/Claude:** reuse the existing structured native-process bridge, target-native paths/essential environment and exact child-tree cancellation. Windows Codex requires shared drive-backed working/report directories. Oversized native model catalogs are parsed under a separate bounded input limit and return only the selected record, not an enlarged credential-file transport limit.

Native auth/cache bookkeeping may occur in the canonical native store; that is not a claim of byte-identical private-home isolation. No protected provider/default-model configuration was manually edited. World coding history preservation is verified independently.

## September 23 verification (historical; current proof above)

- Final code gate: format, lint, types/package builds, **11 architecture tests**, **247 files / 1488 unit/integration/component tests**, PASS.
- Matching normal production frontend/backend build, startup/API smoke, current imported-avatar inputs and compatibility checks PASS. The rebuilt startup-boundary test also passed, retaining its existing budget.
- Isolated Sunlit, Mars, grass close-up, corrected rock close-up and scenic transition captures: zero captured errors. Original transition shader capture also zero errors after a Vite dependency-optimization first-load harness retry. Rock and actor-visibility captures were inspected. Existing Three deprecation warnings are not errors.
- Real discovered installation → setup check → native World session → selected connection → HTTP/MCP discovery → restricted recipe → schema validation → history unchanged/busy released → exact owned session end passed for **OpenClaw WSL, Hermes default WSL, Codex WSL, Codex Windows native, Claude cloud WSL, and Claude Windows native**.
- **Windows Claude: full-path PASS after native reauthentication.** The exact selected Windows launcher created a new owned native session and generated Frozen Starlit Wastes through the HTTP/MCP recipe path; schema validation, coding-history preservation, busy release and exact owned end passed. Persisted binding readback matched the native identity hash and `ended:true`. Earlier expired-OAuth evidence remains historical. The first post-login probe hit a 265-character Windows sentinel path; a model-free reproduction isolated that harness-path limit, and shortening only the disposable probe root let the unchanged product pass. No global long-path setting, account/provider configuration or product code changed. Deep native paths remain subject to Windows accessibility checks.
- All **67 environment media files** and the served feature-bearing WorldRoom chunk were read back and hash-matched. TEST45419's process identities and served build remain unchanged.
- No scripted product-navigation journeys, browser shards or performance journeys. Actual Dance/Bow, repeated creation, Keep/Revert, ordinary movement/chat, audio, comfort and custom-scene appearance remain Aaron's hands-on review.

## September 23 retained test and gates (historical; runtime superseded above)

Fresh normal **TEST45425: http://127.0.0.1:45425/** is left running at untouched Agent Setup with empty browser storage and no saved agent registrations. Five saved-project catalog entries were carried forward without copying native sessions/roster; prior work/history remains retained in its original roots, not falsely claimed fully restored. Local voice currently reports not installed/unverified and remains optional setup; no install or consent was performed.

Only old TEST45421's verified service was stopped before rebuilding its live source dist. Its browser was already absent; its profile, state and saved work remain on disk. Accepted TEST45419, Hermes/OpenClaw gateways, original AgentIntersect and website environments remain protected. Current private owner/evidence kit: `~/.hermes/runs/aiw-world-completion-20260923/`. Do not rebuild served source dist under the new test.

1. **Complete:** Windows-native Claude sign-in and exact selected-agent full-path retest; six-of-six technical native-generation matrix PASS. This does not replace hands-on acceptance.
2. Aaron's affected visual/temporal/audio/comfort and real normal-UI custom-generation review; fix only concrete failures.
3. Add further supplied ambience when available.
4. Only with separate authorization: commit/push, fresh exact-head CI and ready/merge. No publication, installers, releases/download activation or Phase20 by implication. Further bug hunting precedes installers.
