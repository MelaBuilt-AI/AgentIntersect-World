# Vertical-slice repository fixture

A deliberately tiny, deterministic TypeScript repository for later AgentIntersect World vertical-slice tests. Phase 1 records only hierarchy-sized fixture content; it does not index or render this repository.

Expected fixture shape: one package manifest, one source module, and one unit test.
