# Phase 18.5 Codex durable progress

## 2026-07-26 — scope gate

- Confirmed branch `phase/18.5-visual-production` at base
  `998068811cbb6a8770bd50c04bb6cc8268db167c`.
- Read the project rules and current milestone authority before implementation.
- Froze `docs/PHASE_18_5_SCOPE.md` as the first worktree edit.
- Added the machine-readable
  `assets/phase18-5/phase18-5-asset-contract.json`.
- Recorded Luminous Codecraft and all recommended production decisions as
  approved, not pending.
- No implementation file, dependency, external repository, provider, remote,
  commit, or push was touched in this gate.

## 2026-07-26 — concepts and style-bible gate

- Generated three project-authored direction boards and a scored comparison.
- Selected approved **Luminous Codecraft** and generated its deterministic style
  bible; no external assets, services, models, or dependencies were used.

## 2026-07-26 — human-plus-cat hero gate

- Built the smooth modular human and cat assemblies on the shared biped rig.
- Preserved the complete profile inventory and Phase 11 storage migration.
- Captured distinct hero evidence and machine-readable inventory proof.

## 2026-07-26 — full avatar-family gate

- Expanded the same contract to dog and all 12 heads, 12 palettes, shirts,
  hands/paws/claws, feet/paws/claws, fur, tails, and markings.
- Generated a 60-bone rig, 19 reusable actions, consistent facial/speech
  targets, four shared PBR atlases, and three deterministic runtime LODs.
- Proved byte-identical GLB, evidence PNGs, manifests, texture atlases, and
  source inspections in two independent Blender processes. The `.blend`
  container itself retains documented process-volatile metadata.

## 2026-07-26 — semantic repository-kit gate

- Replaced the shared-box presentation with truthful semantic package,
  directory, source, test, documentation, config, data, binary, and symbol
  families plus dependency bridges and evidence/change markers.
- Preserved deterministic transforms, stable selection, shared geometry,
  minimap/camera behavior, and semantic DOM equivalents.

## 2026-07-26 — browser-integration gate

- Added a Playwright Phase 18.5 journey for integrated avatar/repository
  screenshots, console/page-error inspection, and frame/Long Task measurement.
- The managed worker sandbox prevents every installed browser engine from
  starting: Chromium fails on prohibited sandbox/crashpad socket operations,
  WebKit fails to adopt its IPC socket, and Firefox does not complete launch.
  Exact attempt logs are retained in this artifact directory; no screenshot or
  measurement result was fabricated.

## 2026-07-26 — full-verification gate

- Focused Phase 18.5 tests pass 54/54; workspace typecheck passes 38/38;
  package and production builds pass 18/18 and 20/20; architecture passes
  11/11; Storybook production build passes.
- Independent Blender inspection passes, and GLB/contract verification passes
  23/24 checks; only the two browser screenshots prohibited by the worker
  sandbox are absent.
- Two clean Blender generations are byte-identical across all 13 intended
  GLB/PNG/texture/manifest/inspection outputs; `.blend` container volatility is
  isolated and documented.
- The 10,000-object repository preparation measurement passes at 3.962 ms
  maximum. Browser frame and Long Task fields remain truthfully `null`.
- Wrote `PHASE_18_5_REPORT.md` and updated canonical status/design documents.
  No commit, push, provider, release, publication, deployment, or public action
  occurred.

## 2026-07-26 — parent visual correction gate

- Recorded the parent-review correction inside the frozen scope and added
  enforced assembled-avatar quality bands: LOD0 25,000–65,000 triangles, LOD1
  12,000–32,000, and LOD2 4,000–12,000. The final independently recounted
  assemblies are 33,329 / 30,408 / 4,040 triangles with geometrically distinct
  mesh sets and signatures.
- Replaced the abstract concept, style, and repository swatches with
  authoritative Blender renders made from generated project assets: three
  form-bearing concept directions; a multi-panel Luminous Codecraft style
  bible; and an isometric 11-family semantic repository kit.
- Rebuilt the modular family with smoother authored topology, cohesive joint
  transitions, species-specific fur/torso/limb silhouettes, stronger
  faces/muzzles/ears, separate fingers and toes, graphite/midnight panels,
  harness accents, and cyan/violet emissive circuit seams.
- Reworked facial evidence into neutral-lit close-ups for blink, brow up/down,
  eye wide/squint, smile/frown, jaw open, O, E, and MBP. Reworked hand evidence
  into close-ups for open, fist, point, pinch/typing, and expressive spread
  while retaining full-body gestures.
- Completed two independent clean Blender generations and two independent
  source inspections. All 16 intended stable GLB, PNG, texture, manifest, and
  inspection outputs were byte-identical; Blender and inspector processes
  produced their final files before the documented sandbox PulseAudio shutdown
  hang was interrupted.
- Focused verification is green: 35 avatar/renderer tests, 2 contract tests,
  typecheck, production build, architecture checks, Storybook production build,
  formatting, lint (one inherited hook warning), and the 10k repository
  measurement. Avatar verification passes 25 of 26 checks; the sole unresolved
  item is the two real-browser screenshots intentionally left for the parent
  verifier because this worker sandbox cannot launch Chromium.
- No commit, push, publication, deployment, provider activation, external asset
  fetch, or other public/external action occurred.

## 2026-07-26 — parent browser correction gate

- Preserved the real parent browser screenshots and failing measurement. The
  built World reported zero console/page errors, but the retained run remains
  red at 1.7 ms render-work p95, 83.4 ms cadence p95, and a 93 ms Long Task;
  the parent reported 131 ms in the other failing run.
- Added explicit GLB base-color factors for every generated body, panel, face,
  harness, and detail material plus distinct cyan/violet emissive factors.
  Runtime body selection now clones the chosen material instead of mutating a
  shared GLTF material. Independent inspection records GLB SHA-256
  `af8750d8e15b6e564d2937ccc85f52c938695c78785568192018d8b2da7aa6ac`.
- Reduced the activity billboard to 1.9×0.6 at `[3, 3.65, 2]`; tightened the
  third-person distance to 9.5; and bounded/recentered the repository landscape
  so the two avatars and kit compose above the HUD.
- Wired user StartWalk/Walk/StopWalk and agent Think/Explain/Work/Celebrate/Error
  actions into the actual mixer. The renderer now consumes the 0.22-second
  base/upper-body layer, face morph, gaze, and semantic ear/tail pose; reduced
  motion freezes secondary motion without discarding that pose.
- Made LOD policy explicit and observable: builder LOD0, roster LOD1, and World
  distance selection at 6/10 units using the real species-specific LOD2. The
  original 33,329 / 30,408 / 4,040 triangle bands remain unchanged.
- Corrected performance methodology to warm for 45 frames, measure 120
  consecutive normal-motion rAF intervals and Long Tasks without manual render
  work, then collect 24 sparse explicit render samples in a separate phase.
  Reduced-motion demand rendering has a separate assertion.
- RED was observed as eight Vitest failures plus the absent GLB material-factor
  error; a final untracked-layer accumulation regression also failed before its
  bounded correction. GREEN is 3 files / 41 Vitest tests and 3/3 Python
  contract tests.
  Typecheck passes 38/38, production build 20/20, architecture 11/11,
  Storybook production build passes, and the 10k preparation rerun passes at
  2.073 ms maximum.
- Two new clean generator processes plus two independent source inspections
  produced 16/16 byte-identical intended outputs. Blender's `.blend` container
  remains the sole documented volatile output.
- Avatar verification passes 27/28 checks. All asset, material, PBR, rig,
  animation, morph, LOD, hash, and evidence-inventory checks are green;
  `browserEvidenceCurrent` intentionally remains red until the parent reruns
  the corrected browser journey.
- No commit, push, publication, deployment, provider activation, external asset
  fetch, or other public/external action occurred.

## 2026-07-26 — third parent browser correction gate

- Preserved the parent’s latest real screenshots and failing measurement. The
  normal command timed out at 30 seconds; the diagnostic rerun retained 1.2 ms
  render-work p95, 100.1 ms cadence p95, a 110 ms Long Task, zero browser
  errors, and null LOD/render-loop datasets. Blank Chromium calibrated at
  16.7 ms p95, so the failure is treated as app-induced.
- Reframed the World with user `[0, 0, 0]`, agent `[2.6, 0, 0]`, scale `0.82`,
  camera near `[0, 5, 9.5]` toward `[1, 0.7, 0]`, repository centered at
  `[1.1, 0, -2.4]` with scale at most `0.24`, and billboard
  `[2.6, 3.25, 0]` at `1.65×0.5`.
- Fixed World DPR at 1.0. Both initial avatars now sit beyond the 10-unit cutoff
  and render the real 4,040-triangle species-specific LOD2 rather than the prior
  pair of 30,408-triangle LOD1 assemblies.
- Stopped the separate DOM movement rAF outside StartWalk/Walk and removed two
  live HUD backdrop-blur compositor passes over the WebGL surface.
- Configured clones physically remove hidden/unselected render meshes after
  material selection. Scene memoization uses selection values plus LOD;
  head/eye/ear/tail and morph targets are cached once; and one mixer updates
  once per avatar. LOD2 consumes one full semantic clip while LOD0/LOD1 retain
  masked base/upper-body composition.
- `userAvatarLod`, `agentAvatarLod`, and `renderLoop` are written directly to the
  real WebGL canvas dataset. The measurement now emits cadence
  median/p90/p95/max, count above 16.8 ms, and bounded Long Task summaries.
- RED was 14 focused failures. GREEN is 3 files / 48 Vitest tests plus 3/3
  Python contract tests. Typecheck passes 38/38, production build 20/20,
  architecture 11/11, formatting/lint, Storybook, and the 10k preparation
  journey pass; the latter measured 1.442 ms median and 2.197 ms maximum.
- The asset payload was not regenerated. Verifier status remains truthfully
  27/28 with only `browserEvidenceCurrent` red until the parent replaces the
  failed browser evidence.
- No commit, push, publication, deployment, provider activation, external asset
  fetch, or other public/external action occurred.

## 2026-07-26 — fourth parent targeted correction gate

- Preserved the fourth parent’s real screenshots and failed measurement. Browser
  errors were empty; composition passed; the actual canvas reported user
  `LOD2`, agent `LOD1`, and `continuous`; render-work p95 was 0.6 ms; cadence
  was 50.1 ms median, 66.7 ms p90, 83.3 ms p95, and 100 ms maximum with
  120/120 samples above 16.8 ms; Long Tasks passed the maximum threshold at
  92 ms but numbered 118 and totaled 6,952 ms.
- Isolated the remaining blocker to the bounded forward journey moving the
  camera enough to put the agent inside the prior 10-unit World cutoff.
- Changed only World automatic distance policy: LOD0 through 6 units, LOD1
  above 6 through 9 units, and LOD2 beyond 9 or for non-finite inputs. Builder
  remains forced LOD0 and roster remains forced LOD1.
- Added exact-boundary coverage for 9, just above 9, positive/negative infinity,
  and NaN, plus a pure camera-distance regression proving both the user and
  agent remain LOD2 after the journey’s 260 ms forward movement.
- RED was 2 failures / 28 passes. Focused GREEN is 3 files / 49 Vitest tests.
  The passing composition, direct datasets, normal-motion loop, 30-second
  timeout, thresholds, DPR 1, physical pruning, one mixer, cached targets,
  material behavior, and generated assets were not changed.
- Final non-browser gates pass: formatting, lint, 38/38 TypeScript tasks,
  20/20 production builds, Storybook production build, 3/3 contract tests,
  and the no-IPC architecture check plus 11/11 architecture tests. The normal
  architecture wrapper encountered the documented sandbox `tsx` IPC `EPERM`.
  Avatar verification remains truthfully 27/28 with only
  `browserEvidenceCurrent` red.
- No commit, push, publication, deployment, provider activation, external asset
  fetch, or other public/external action occurred.

## 2026-07-26 — final parent hardware acceptance gate

- Copied the accepted 1600×1000 Edge hardware screenshot to
  `browser-world-edge-hardware.png` and recorded its SHA-256
  `3595c73a3e61d0805e6b554356fffe36f1a3f27cc343a4cb9e41bafa84cc9c9d`.
- Added canonical Edge 150 / NVIDIA GeForce RTX 5070 Ti evidence: zero errors,
  both LOD2, continuous loop, 45 warmup frames, 120 cadence samples, 10.2 ms
  cadence p95, 0.2 ms render-work p95 over 24 samples, and zero Long Tasks.
  Parent visual QA passed.
- Fingerprinted the current avatar renderer, World renderer, renderer index,
  avatar-system source, runtime GLB, and accepted screenshot. Hardware evidence
  validation fails closed on any drift, failed evidence, renderer
  misclassification, or unchanged-threshold breach.
- Software-emulation evidence preserves raw median 33.3 / p95 50.0 ms cadence
  as `cadencePassed=false`; render-work p95 0.3 ms, Long Tasks 0, functional
  state, and hardware evidence pass. It is not cadence authority.
- RED was one missing TypeScript module suite and three missing Python verifier
  functions. GREEN is 4 files / 54 Vitest tests plus 6/6 Python tests.
- TypeScript 38/38, build 20/20, formatting, lint, Storybook, no-IPC
  architecture check plus 11/11, and avatar verification 28/28 pass. Full
  Vitest completed at 106/115 files and 604/636 tests; all 32 failures and 12
  unhandled errors are inherited listener/child-process sandbox `EPERM`.
- Exact `measure:phase18.5` completed both builds but the sandbox denied the
  configured local-server startup before Playwright. A direct server start
  failed identically. No software or hardware result was fabricated.
- No commit, push, publication, deployment, provider activation, external asset
  fetch, or other public/external action occurred.

## 2026-07-26 — final full-gate regression correction

- Preserved the accepted Edge/NVIDIA visual and performance evidence and all
  five production fingerprints. No hardware-evidence hash was rewritten.
- Added explicit nullable background deselection to the repository R3F canvas,
  replacing the accidental fixed-coordinate hit dependency.
- Bounded graph projection before metadata enrichment/preparation: 2,000
  supported objects for WebGL and 160 for semantic fallback. The full semantic
  model/search remains truthful, and focused files outside the projection cap
  are projected individually.
- Added a deterministic 1,000+-object aggregate render tier with DPR 1,
  antialiasing disabled, and shared basic family materials. Detailed small-world
  semantic silhouettes and PBR materials remain unchanged.
- Kept all 12 roster LOD1 avatars visible while bounding the fixture surface to
  56rem × 24rem. Optional compact 3D previews now wait 1,400 ms, so opening the
  Agents panel cannot consume the unchanged 1,200 ms operation-cancel window.
- RED was a missing web policy module plus two missing renderer helpers, while
  18 existing tests remained green. GREEN is 5 focused Vitest files / 41 tests
  plus 7/7 Python contract tests.
- Corrected verifier gate ordering: semantically current formatted inspection
  JSON is no longer rewritten. Formatting, lint, TypeScript 38/38, build 20/20,
  Storybook, no-IPC architecture plus 11/11, and avatar verification pass.
- Full Vitest completed at 107/116 files and 610/642 tests; all 32 failures and
  12 unhandled errors are listener/child-process sandbox `EPERM`. The exact
  Phase 18.5 command completed both builds and then hit the same configured
  local-server startup denial.
- The seven-test Playwright regression lane could not start its configured
  server in this worker sandbox. Parent browser rerun and canonical `pnpm check`
  remain required before sealing this correction.
- Canonical `pnpm check` passed formatting, lint, package builds, root
  TypeScript, and 38/38 workspace typechecks, then stopped at the sandbox-denied
  `tsx` IPC socket before its later aggregate lanes. The same architecture
  script passes through the no-IPC `node --import tsx` invocation plus 11/11
  architecture tests.
- No commit, push, publication, deployment, provider activation, external asset
  fetch, or other public/external action occurred.
