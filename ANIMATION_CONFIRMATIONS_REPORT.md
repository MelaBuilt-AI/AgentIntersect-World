# AgentIntersect World — Animation Confirmations Report

**Status:** CORE AND UI USER-ACCEPTED; PRIVATE COMMIT/PUSH AUTHORIZED; EXACT-SHA CI REQUIRED
**Accepted:** 2026-09-02
**Repository:** `/home/user/AgentIntersect-World`
**Base:** private `main` at `ada73b14327440ffed5820c34d047edb9ee0c4c3`

## 1. Scope and authority

Animation Confirmations followed completed Phase 19 Task 16 and precedes Phase 20. Aaron's human review is the sole semantic authority for model-local gesture meaning. Automated evidence proves identity, runtime wiring, trigger timing, and deterministic behavior; it does not substitute for first-hand visible motion acceptance.

This milestone does not authorize commit, push, PR, merge, tag, release, publication, deployment, visibility change, provider/profile mutation, protected-service mutation, or Phase 20 implementation.

## 2. Human-reviewed animation authority

Aaron supplied a complete annotation export covering:

- 23 imported models;
- ten reviewed gestures per model;
- 230/230 resolved semantic choices;
- zero remaining, uncertain, or unsupported semantic decisions;
- 69 previously accepted model-local Idle/Walk/Run mappings.

The original 133,414-byte export is preserved unchanged with SHA-256 `79df72e163ca956959e9afe4a3b58b05dc4bc7d00ce57ab821b664e842811cb8`.

After Aaron explicitly approved transparent evidence completion, a SHA-bound derived annotation added only factual evidence references, factual notes, and one timezone-aware confirmation timestamp. Its SHA-256 is `a1b567f89803a7fe44099cec7341a7bbe1b2465211d4ae83cd32a407a8fce5f7`.

The separate 230-row proposal and verification receipt are preserved under:

`artifacts/avatar-replacement-evidence/world-animation-operator-review-v2/`

## 3. Runtime integration

The deterministic generated authority contains:

- 23 models;
- 13 semantics per model;
- 299 model-local mappings;
- 299 pass decisions;
- 23 model-local Dig mappings.

Imported avatars map truthful coding `Work` to their own reviewed Dig clip. Dig:

1. requires structured tool/edit evidence for the exact current Repository City object;
2. starts only after matching safe-point arrival;
3. loops while coding remains active;
4. clears on completion, failure, cancellation, retargeting, stale authority, or coding-state loss;
5. returns to model-local Idle;
6. preserves coding semantics while reduced motion pauses visible mixer motion.

The historical Phase 18.5 visual/temporal evidence remains frozen against its historical mapping file rather than being silently regenerated from current mappings.

## 4. Initial technical proof

The integrated slice passed:

- focused Animation Confirmation tests: 23/23;
- impacted avatar/runtime tests: 55/55;
- deterministic generation and historical evidence checks;
- Python compilation;
- repository typecheck: 38/38 tasks;
- full Vitest: 164 files / 1,054 tests;
- production package/web builds;
- exact desktop coding browser journey: 1/1;
- desktop, reduced-motion, and no-WebGL matrix: 3/3;
- Git LFS verification and diff hygiene.

## 5. First-hand acceptance history

### 5.1 Initial movement failure

Aaron completed setup and the real Codex edit, but the agent did not move or play Dig. The failed state was frozen before cleanup.

A fresh real Codex `0.149.1` stream proved `file_change` supplied an absolute path under Codex's workspace clone. Repository City was indexed from the equal canonical clone, so the sibling absolute path was rejected by exact-root resolution.

Strict RED failed 1 of 18 adapter tests on that exact absolute-versus-relative mismatch. The smallest correction converts only paths contained by the exact Codex native-session root to repository-relative paths; outside-root paths remain fail-closed. GREEN passed 18/18, and impacted work-focus proof passed 4 files / 41 tests.

### 5.2 Multi Agent instruction superseded

The first corrected checklist unnecessarily selected Multi Agent. One accepted Codex correctly left `entryReady: false` because Multi Agent requires two accepted agents. This was classified as a superseded test instruction, not a product failure. The requested Dig test requires one Codex and belongs in Single Agent mode.

### 5.3 Single Agent chat failure

The first Single Agent candidate reached Repository City, then displayed `chat unavailable_` on send. Retained state contained one ready Codex session and accepted avatar but zero messages and zero events, proving pre-network browser rejection.

The exact cause was a stale frontend guard that rejected every Single Agent session whose adapter was not Hermes. Strict RED reproduced it. The smallest correction removed only that adapter restriction while retaining bounded non-empty text and the existing exact-session SSE path.

GREEN passed, the impacted client/UI set passed 2 files / 45 tests, and a separate real production local-server proof returned SSE 200, accepted one user and one assistant message, recorded three tool start/completion pairs, completed the isolated edit, and produced exact `src/DigAcceptance.ts` work focus with a non-null movement request.

### 5.4 Core movement and Dig PASS

In a fresh normal-product Single Agent lane, Aaron completed steps 1–7 and reported the full first run worked. This first-hand PASS covers:

- real Codex connection and response;
- Repository City load;
- movement toward the exact `src/DigAcceptance.ts` object;
- Dig beginning only after arrival/coding;
- visible animated Dig rather than Idle, frozen pose, or T-pose;
- grounded presentation;
- completion clearing Dig and returning to Idle.

### 5.5 No-op repeat classification

After refresh and a new Codex connection, Aaron repeated the exact already-completed edit and saw no second movement. Both turns completed and persisted. The first native session had one `FileChange`; the second had only two command reads and no `FileChange` because the workspace already contained `animationAcceptance = "verified"` and the exact comment.

The second run therefore had no new coding trigger. It is a no-op repeat, not a refresh movement regression, and does not invalidate the first PASS.

### 5.6 Naming and status follow-up

Read-only DOM evidence confirmed the actual agent identity was `Codex` in the roster and message input, while assistant transcript headings and global activity text still used hardcoded `Mr Fluff`. The HUD also passively displayed external Structured World Actions refusal even while the separate server-owned repository-focus movement path worked.

Strict RED reproduced both presentation defects. The minimal correction:

- uses the selected Single Agent recipient for assistant transcript headings;
- projects the global activity label from the selected agent name;
- stops passive external World Actions capability refusal from overwriting movement status;
- retains explicit movement request, outcome, and transport errors.

Focused proof passed 2/2; impacted UI/client/avatar proof passed 5 files / 80 tests; web typecheck, Prettier, diff hygiene, and production build passed.

Aaron then returned `PASS UI check`. Supporting read-only DOM evidence showed:

- assistant heading: `Codex`;
- activity: `Codex completed the request`;
- message recipient: `Message Codex`;
- roster identity: `Codex`;
- false passive movement-warning count: zero.

## 6. Final acceptance matrix

| Criterion                                      | Result                       |
| ---------------------------------------------- | ---------------------------- |
| 230/230 human semantic decisions preserved     | PASS                         |
| 299 model-local runtime mappings generated     | PASS                         |
| Exact model/source/semantic binding            | PASS                         |
| Real Codex Single Agent chat                   | PASS                         |
| Exact Repository City object resolution        | PASS                         |
| Movement begins from structured edit evidence  | PASS                         |
| Dig begins only after arrival/coding           | FIRST-HAND PASS              |
| Dig visibly animates and remains grounded      | FIRST-HAND PASS              |
| Completion returns to model-local Idle         | FIRST-HAND PASS              |
| Selected Codex identity in transcript/activity | FIRST-HAND PASS              |
| Passive capability warning absent              | FIRST-HAND PASS              |
| Protected Hermes/OpenClaw unchanged            | PASS                         |
| Git delivery                                   | NOT AUTHORIZED / NOT STARTED |
| Phase 20                                       | NOT AUTHORIZED / NOT STARTED |

## 7. Retained evidence

Primary acceptance roots:

- Original failed movement lane: `/home/user/.hermes/runs/aiw-animation-manual-20260902T101751-0400/`
- Locator-corrected lane: `/home/user/.hermes/runs/aiw-animation-retest-20260902T184034-0400/`
- First Single Agent chat-failure lane: `/home/user/.hermes/runs/aiw-animation-single-retest-20260902T190053-0400/`
- Core PASS and mixed-state lane: `/home/user/.hermes/runs/aiw-animation-single-retest2-20260902T191648-0400/`
- Final UI PASS lane: `/home/user/.hermes/runs/aiw-animation-ui-check-20260902T200459-0400/`

The final UI receipt records one Codex session, one accepted user message, one completed assistant reply, exact Codex labels, zero false warning, and a hash-only candidate-state manifest without transcript or credential bodies.

## 8. Repository and next gate

Aaron authorized commit and private push of the complete accepted Animation Confirmations slice with exact-SHA CI. This report intentionally does not embed its future commit SHA or workflow ID; the final immutable delivery receipt remains external to avoid a recursive status commit.

Delivery must stage the complete final path set, verify the exact index, commit and push only to private `main`, and require the final branch-tip SHA's workflow and check runs to succeed before calling delivery green.

Phase 20 remains not started and not authorized. Its dependency is now satisfied, but it requires a separate explicit authorization after Animation Confirmations delivery is resolved.
